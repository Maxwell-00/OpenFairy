# Reviewer Handbook — for the Claude taking over this seat

Written 2026-07-03 by the Claude (Fable 5) that held this role from the first design doc through M2-04. If you are a Claude (Opus 4.8 Max, Fable 5 High, or later) opening a fresh Cowork conversation on this project: this file is your inheritance. Read it before acting. It is deliberately on disk and versioned — the institutional memory of this project lives in files, not in any one model's context.

## 1. Your role in the three-party loop

**Codex (ChatGPT 5.5) implements. ChatGPT-5.5-Pro reviews first. You gate, verify, and countersign.** The owner (Maxwell, communicates in Chinese, commits via GitHub Desktop on Windows) carries artifacts between parties.

The loop, per task slice:
1. A task brief (`tasks/M<x>-<nn>-<name>.md`) is gated by you *before* dispatch — spec-fidelity audit (see §4).
2. Owner dispatches to Codex → Codex delivers + writes `tasks/M<x>-<nn>-work.md` → **owner commits before you review** (non-negotiable, see §3).
3. ChatGPT-Pro writes `tasks/M<x>-<nn>-review.md` (primary review).
4. You verify at code level and **countersign** (append to the review file), apply reviewer-owned doc edits, patch the next brief, thread carry-ins.

Division of judgment, learned from experience: ChatGPT-Pro's delivery reviews reach correct verdicts but are **trust-based** (work report + owner artifacts, rarely code reading with file:line evidence). Its briefs are structurally good but weak on **spec fidelity** — it has invented config values not in any spec (`regions: [global]`, `local-preferred`), missed derivation subtleties (labels must compose over the WHOLE assembled prompt, not the current input), used wrong owner-environment examples (`home_regions` is `[cn]`), and skipped evals.md suite registration. Your verification and brief-gating exist precisely to cover these holes. Also: Codex and ChatGPT are the same vendor family — correlated blind spots are real (the dotted-tool-name incident was an OpenAI-convention blind spot). You are the cross-vendor eye.

**Cost directive from the owner:** delegate sizable self-contained verification (delivery code-reads, multi-file audits) to an **opus subagent** with a tight evidence-checklist prompt; do inline only what is too small to amortize a subagent cold start. The subagent verification prompt template is preserved in §8 of this handbook (conversation records do not survive account/session changes; the file does).

## 2. Where everything lives

- `docs/` — **normative**. PRD (FR/NFR ids), ARCHITECTURE, ROADMAP (milestone gates), DECISIONS (20+ ADRs, append-only law), 12 specs. Specs win arguments.
- `tasks/` — the implementation trail: brief → work report → review (+ countersignature). **Read the newest 2–3 review files first when orienting**; every incident and its standing rule is recorded there.
- `CLAUDE.md` / `AGENTS.md` — identical twins (sync with `cp CLAUDE.md AGENTS.md` after edits); the coding-agent contract.
- `.auto-memory/` in the parent folder — Claude session memory (compressed project history). Background, not authority; files win.
- Parent folder (`E:\...\Fairy\`) — non-repo material (`ChatGPT_Suggestions/`, `docs-zh/` owner-maintained translations — never edit, flag re-translation when English docs change).

## 3. Environment landmines (each cost us a debugging round)

1. **Mount staleness:** the Cowork mount serves stale/truncated reads of files recently written on the Windows side — looks like mid-token corruption. Never trust working-tree reads for fresh files. Read via `git --no-optional-locks show HEAD:<path>`. Never `Edit` from a possibly-stale view — compose full content from git truth and `Write`.
2. **Git locks:** plain `git` commands leave orphan `index.lock` on this mount. Always `git --no-optional-locks`. Never commit/checkout/restore from the sandbox — the owner commits locally.
3. **Deletes need permission** and the owner will ask what you're deleting — list files and reasons *before* attempting.
4. Owner environment: Windows + Docker Desktop (WSL2), pnpm via corepack (`packageManager` pinned; new native build scripts need `pnpm approve-builds` **and the approval file must be committed** or CI breaks), conda/uv habits (Python side only), provider keys via `FAIRY_SECRET_<NAME>` env in the gateway terminal, `pnpm fairy <cmd>` for CLI.
5. **CJK/encoding verification through Windows terminals is unreliable.** M2-05 incident: Codex was told to remove a mojibake regex alternative and verify with `rg "<mojibake>"`; the target string got re-mangled by the terminal before the search ran, "no matches" was reported honestly, and the mojibake survived two green reviews. Rule: any check involving non-ASCII target strings must be written with `\uXXXX` codepoint escapes inside a `node -e` script (fully ASCII on the command line), never by pasting raw CJK. Reviewers: verify such claims by `git show` + grep yourself, not from work-report tails. Second-order trap (M2-05c): the escape-form check is shell-sensitive — bash double quotes collapse `\\u` so node decodes to raw CJK and reports a spurious FAIL; PowerShell passes it through as written. Fail-closed design means the artifact is always FAIL, never false PASS — if a check fails unexpectedly, suspect your shell quoting before suspecting the code. Also: JS `\b` is inert adjacent to CJK — never wrap CJK regex alternatives in `\b...\b` (the pre-M2-05c OTP anchors silently failed on space-separated forms because of this).

## 4. Review method

**Delivery verification (countersignature standard):** verify the brief's critical items at code level with file:line evidence, via `git show`/`git grep` at the delivery commit. Always run the checks the primary review merely recommends. Standard invariant sweep: exactly one TurnRunner; no vendor SDKs (`git grep "from \"openai\|@anthropic"`); no provider strings in `packages/kernel`; no new event types outside `docs/specs/protocol.md` §2 registry; schemas+fixtures for anything new; dep-cruiser rules intact.

**Brief gating checklist:** every config key/value exists in a spec (or is explicitly proposed as a doc edit); derivation/composition rules stated where labels or budgets flow across components; carry-ins from the last review threaded in; eval suites named per `docs/specs/evals.md` registry ("new capability lands with its suite"); boundaries exclude the next slices explicitly; owner-env examples correct; acceptance is command-runnable, both CI OSes; report-back format kept (file tree / tails / decisions / **spec ambiguities — an empty list is suspicious** / proposed doc edits / owner checklist).

**Docs are reviewer-owned.** Codex and ChatGPT propose; you apply. Apply promptly after countersigning or edits get lost between tasks. Event types must be registered in protocol.md *before* implementation; additive-minor evolution rules apply; config keys get spec-registered.

**Reward disclosure.** Codex has honestly declined to fake transcripts and disclosed incomplete verification steps; this norm was cultivated deliberately. Praise it in reviews; never punish honest gaps — punish silent ones.

## 5. Standing engineering rules (incident-derived; enforce, don't relitigate)

Origins in the corresponding `tasks/*-review.md` addenda.

1. Source-first workspace: `exports.import` → `src/index.ts` everywhere; never `dist/` exports; never gate a test on building a sibling.
2. One execution world: everything runs via tsx (`node --import tsx entry.ts`); spawning plain `node` on `.ts` or `dist/` is a bug until M5 packaging.
3. Crash tests kill the process under test, never a wrapper (POSIX orphans defeat the test).
4. Every environment probe carries a deadline; in-container probes carry their *own* deadline (never rely on resolver/library defaults — DNS under `--network none` is nondeterministic); docker-dependent tests declare explicit generous timeouts; CI pre-pulls the sandbox image.
5. The mock provider must reject what real providers reject (wire-format constraints live in the mock, or e2e green is meaningless). Corollary: owner manual tests on real providers are irreplaceable — they caught what every green CI hid.
6. Provider quirks are fixed in the transport + a fixture, never as kernel special-cases (vitest guard exists; upgrading it to a dep-cruiser rule is an open nice-to-have).
7. Local green ≠ CI green only when environments diverge; since the single-world fix they match — trust local runs, but CI on both OSes is the gate. `windows-latest` cannot run Linux containers; skip container tests there with visible reasons.
8. Labels: residency is a closed hard enum; `prefer_local` is a hint, never gating; escalation is table-driven and one-way; effective labels derive over the whole assembled prompt including the memory digest; a denied provider receives zero request bytes.
9. Briefs scope `git grep` acceptance assertions to code paths (`-- packages apps`), or they match the brief itself.
10. CI is secret-free and Ollama-free; live conformance is owner-run (`pnpm conformance --model <id>`); verdicts distinguish pass/fail/skip/degraded — never overload PASS.

## 6. Current state (as of writing — verify against git log and newest review files)

M0–M1 closed (text spine). M2 functional slices M2-01 through M2-08 are closed, including M2-06b acceptance-debt hardening, M2-05b affect cache/negative-feedback, and M2-05c encoding hygiene. M2-06 perception service is closed at `ceb2a74` (mock artifact pipeline, `vision.describe`/`vision.ocr`, `perception.quarantine-v0`; real vision/OCR provider + `perception.vision` role wiring remain future work). M2-07 context ladder L4/L5 is closed at `dd3b0a6`, and its two exit carry-ins were discharged by M2-08. M2-08 Chronicle v1 + dream-cycle v0 is closed at `5f3ef12`; consolidation is deterministic/no-model, canary + contradiction benchmarks remain visibly deferred, and synthetic session dirs under `sessions/` are an accepted accounting note. **M2 IS CLOSED** — exit gated 2026-07-08 (`tasks/M2-exit-review.md`, verdict `M2 CLOSED WITH EXPLICIT DEFERRALS`; all 13 named suites green and per-task countersigned). **The exit review's deferral register carries binding landing gates** — S4 ≥20 real sessions → M4 entry; persona frozen judge ≥90% → M4 exit / first critic wiring; sqlite-vec vs LanceDB ≥200k benchmark → brief-gate precondition of any vector-implementation task; memory canary + contradiction benchmark → model-backed consolidation task; friction canary soak thresholds → M5. Whoever gates those future briefs must enforce these expiries; carrying a deferral past its landing gate requires an explicit recorded decision in that gate's review. **M3-01 (voice protocol + loopback transport) is CLOSED** — gated at `683d5d0`, delivered `bf6896e`, countersigned 2026-07-09 (`tasks/M3-01-review.md`; opus subagent 14/14, weakening scan clean). Standing rulings from that close: spoken "remember" floors at `personal` and is **held** by MemoryGate (`personal_default_hold`), never auto-written — voice does not weaken admission (hold→write needs an explicit confirmation flow, future slice); `docs/specs/voice-pipeline.md` **exists since M0-01** (do not trust "missing spec" claims — verify negative-existence with `ls-tree` before writing it into a gate). **M3-02 (duplex speech transport + conformance interface) is CLOSED** — gated at `adf8b33`, delivered `fe8afc3`, countersigned 2026-07-09 (`tasks/M3-02-review.md`; opus subagent 12/12, weakening scan clean). Standing rulings from that close: duplex frame labels are **advisory only** — the gateway-side profile floor is authoritative via the one-way `clampVoiceFrameLabels` (raise-only, enforced on emitted events); duplex control frames are a **third frame family** (worker plane) outside both the event registry and `packages/protocol/frames/`, never in JSONL; both voice ops share the single `#submitVoiceFinalTranscript`→`#acceptTurnInput` construction site — keep it single in every future voice slice; `asr-cancelled` joined the mark vocabulary additively. **M3-03 (local WebSocket speech transport + loopback conformance) is CLOSED** — gated at `341eb9c`, delivered `524dd33`, countersigned 2026-07-09 (`tasks/M3-03-review.md`; opus subagent 13/13, weakening scan clean with one justified source-scan relaxation). Standing rulings from that close: **every voice listener requires token auth on connect (`4401` otherwise, zero frames processed) and binds hard-coded loopback — an unauthenticated voice port is an unlabeled ingress and is forbidden; no host/interface config key may exist until a TLS+auth slice gates it**; the WS binary JSON-header envelope is the v0 conformance shape only — the compact "4-byte channel id + payload" framing is reserved for the real-audio/Opus slice (recorded reconciliation, voice-pipeline status note); the M3-02 interleave carry-in is discharged (two-stream per-stream FIFO test, genuinely discriminating); one `ws` library workspace-wide. **M3-04 (speech worker process scaffold + Python mock worker) is CLOSED** — gated at `db0556f`, delivered `e3e8089`, countersigned 2026-07-10 (`tasks/M3-04-review.md`; opus subagent 12/12, weakening scan clean, additions-only). Standing rulings from that close: **Windows stdio hygiene is normative for any stdio worker** (spawn `python -u -B` with `shell:false` and repo-external cwd; worker writes via `sys.stdout.buffer` + per-message flush, never `print`; TS reader strips trailing `\r`; both pipes drained concurrently with bounded buffers); stdio NDJSON (`fairy.speech-worker.v0`) is the v0 wire for gateway-supervised local children — §2's authenticated local WS remains the remote-worker transport; `asr.script`/`tts.script` are mock-conformance-only kinds; worker executable/script paths are repository-controlled, never user/config-supplied; `FAIRY_TEST_PYTHON` is code-enforced test-only argv0; worker failure = `progress.update` stage `voice.worker.failed`, no new event types. **Named carry-in with landing gate: minimum supported Python version (recommend ≥3.11) must be declared + probe-enforced + given a CI floor lane in the FIRST REAL speech-worker slice — thread it into that brief at gate.** `docs-zh/` was removed by the owner; the per-close re-translation flag is discontinued unless recreated. **M3-05 (MiniMax T2A v2 non-streaming TTS — FIRST REAL PROVIDER) is CLOSED** — gated at `1fc2f98`, delivered `a2d7f6e` (cumulative: `80726ea`+ci fix+hardening fix), owner-live verified, countersigned 2026-07-10 (`tasks/M3-05-review.md`; opus subagent 13/13). Standing rulings from that close: **speech providers share one clearance law** — `canRouteToClearance` (pure parameterization of `canRouteToModel`, zero semantic change to model routing; any future clearance consumer routes through it, never a copy); **the neutral artifact store is `packages/artifacts`** (verbatim extraction; perception tests byte-identical + SHA-pinned; artifact kinds are TS-union additive, the canonical schema has no kind constraint); **provider workers get per-worker source scans** — the mock worker's no-network scan is never weakened; **provider credentials travel via one narrow env key** (deliberately outside `FAIRY_SECRET_*` broad inheritance), proxies disabled in code AND env, endpoints are closed adapter-owned profiles, redirects fail closed; **TTS artifacts inherit the visible text's labels — synthesis never declassifies**; **Python floor `>= 3.11` is settled** (probe + handshake + CI floor lanes; M3-04 carry-in discharged); real-provider close is owner-live evidence only, never CI. **Named carry-in → M3-06 gate: `server.ts` absorbed +365 lines this slice (+242 at M3-04) — the next speech brief must contain a speech-coordinator extraction decision (own module or recorded justification) before more provider logic lands there.** ROADMAP/PRD/ARCHITECTURE doc proposals deferred to M3 exit consolidation. **Next: gate the M3-06 brief** (local ASR worker per the agreed M3-05..13 plan: pick one of faster-whisper/FunASR/SenseVoice; must carry pip/uv dependency management for `workers/speech`, model download/caching with CI staying mock, `speech.asr` role + clearance, ASR-side audio artifact input, and the coordinator-extraction decision). Docs passes through M3-05 are applied. Parallel Cowork conversations exist — check their outputs in `tasks/` rather than assuming this file knows everything.

M2-05 landed clean on its hard rails (deterministic affect, substance invariance test-gated, off switches, no model-set mood). Note for future suite-reading: legacy gateway E2E now default `persona.enabled=false`; enabled-persona coverage lives in the dedicated persona E2E + kernel/evals tests.

## 7. Working with the owner

Communicate in Chinese; keep docs/code English. Be direct about verdicts; the owner values honest root-cause narration over reassurance, and has consistently accepted "my previous fix caused this" when said plainly — say it plainly (this seat's own misfixes are logged in the M1-01 addenda; keep that tradition). Explain money-costing steps before running them. When CI fails: read the log first, locate the root cause, fix from git truth, and record the incident as a rule in the relevant review file. When the owner asks "能不能换掉你" — the answer that served well: give the honest structural argument, then defer; the review-file discipline, not any particular model, is the project's memory

## 8. Subagent verification prompt template (refined M2-01 → M2-08)

Every code-level countersignature in M2 was produced by an opus subagent given this shape. Reuse it; the refinements each came from an incident.

```text
Read-only code-level cross-check of the <TASK> delivery in the OpenFairy repo at <repo path>.
ENVIRONMENT RULES (hard): mount serves stale/truncated working-tree reads — read ONLY via
`git --no-optional-locks show <COMMIT>:<path>`, search ONLY via
`git --no-optional-locks grep <pat> <COMMIT> -- <paths>`. Never use the Read tool on repo
files; no git write commands.

Context: commit <COMMIT> ("<msg>", parent <PARENT>) delivers "<slice>" per brief
`tasks/<brief>.md` (read at <COMMIT> — it contains reviewer-gate clauses you must verify
hard: <list the gate patches you added at brief gate>). Key files: <from the commit stat>.
The primary (ChatGPT) review accepted trust-based; you are the code-level countersign.

Verify with file:line evidence (quote decisive lines):
1..N. <one numbered item per acceptance area; ALWAYS include:>
  - the reviewer-gate clauses from the brief gate, marked "(gate item)" — highest priority;
  - non-vacuity spot-checks: "would this test fail if the property broke?" — demand the
    strongest assertions quoted (request counts, marker-presence-before-partition, etc.);
  - BOUNDARIES + INVARIANTS at <COMMIT>: one TurnRunner; no vendor SDKs; no provider strings
    in packages/kernel/src; governance.ts/persona.ts/<other frozen files> untouched vs parent;
    no docs/docs-zh in commit; zero raw CJK in new src (new regex literals must be \u-escaped
    per M2-05c); no new canonical event types (grep emitted type: strings vs
    packages/protocol/schemas/registry.v1.json); no new eval suite names beyond the brief's;
  - WEAKENING SCAN: diffs of pre-existing test files — no deleted/loosened assertions;
    quote anything suspicious.

Return: numbered verdicts with evidence; prominent FAILED/PARTIAL/vacuous list; findings
outside the checklist; one-line overall verdict.
```

Grading the return: PARTIALs with "logic present, test missing" become carry-ins threaded into the next brief's Deliverable 0 (see M2-07 → M2-08 for the worked example). A FAIL on a gate item is a blocker regardless of CI color.

## 9. Seat log

- 2026-07-03 → 2026-07-08, Claude Fable 5 (first seat, two Cowork accounts' conversations): design docs through **M2 exit gate**. M2 closed with explicit deferrals — the deferral landing gates in `tasks/M2-exit-review.md` are this seat's most important handover; enforce them at the named gates. Incident-derived rules absorbed into §3/§5 during this tenure: mojibake/encoding class (§3.5), CJK-\b inertness, shell-quoting fail-closed behavior, mount tail-truncation signatures (git status showing phantom M/D on runtime files at session start — always hash-compare against HEAD before concluding anything).
- 2026-07-08 →, Claude Fable 5 (second seat, new Cowork account): took over post-M2-exit; previous seat's memory bundle restored. First act: M3-01 brief gate (`tasks/M3-01-brief-review.md`, ACCEPTED WITH REQUIRED EDITS, applied in place). Confirmed §1's spec-fidelity warning holds: the drafter's suggested speech payload shapes contradicted the already-shipped `speech.*.v1` schemas (invented `stream_id`/`segment_id`/mark enum; treated complete schemas+fixtures as missing) and proposed an invalid `provenance: "speech"`. Rule reinforced: before gating any protocol-touching brief, diff its suggested shapes against `packages/protocol/schemas/` at HEAD first. Also found & flagged: the committed handbook carried an embedded NUL-byte run at §7's tail (since `df76c45`) — stripped in this working-tree version; text tools were treating the file as binary. Watch for this corruption class in any file composed from a working-tree read.
  M3-01 countersigned/closed 2026-07-09; two seat lessons from it: (1) verify subagents' negative-existence claims yourself (`ls-tree`) — my gate wrote a false "voice-pipeline.md does not exist" into the brief and three parties repeated it; (2) when a gate edit sets a floor/invariant (RE-4's personal floor), sweep the brief's other acceptance lines for collisions (the `memory.written` line survived and Codex had to resolve the conflict — correctly — at implementation time).
  M3-02 countersigned/closed 2026-07-09 (12/12; the RE-2 label clamp the gate demanded landed as a clean one-way rank-max, asserted on emitted events). Gate→delivery loop is now tight: every RE clause verified in code, no new incident class.
  M3-03 countersigned/closed 2026-07-09 (13/13; auth-before-handler and hard-coded loopback binding spot-checked directly). The gate→delivery loop has now held for three consecutive slices with zero FAILs at countersign — the RE-clause + subagent-checklist method is doing its job; keep the load-bearing-clause spot-check habit.
  M3-04 countersigned/closed 2026-07-10 (12/12; RE-2 stdio-hygiene evidence spot-checked directly — spawn args, buffer-write path, CR strip). Four slices, four clean countersigns; Codex now routinely builds beyond the gate clauses (version-mismatch rejection, repo-external cwd, correlation invariants) — keep praising it in reviews. New named carry-in with landing gate: Python version floor → first real speech-worker brief.
  M3-05 countersigned/closed 2026-07-10 (13/13; governance.ts refactor + worker proxy/TLS code spot-checked directly). First real provider closed with owner-live evidence; the TS-direct-adapter misjudgment this seat made in planning was corrected by ChatGPT's architecture argument and is recorded in the gate — the cross-vendor eye works in both directions. New carry-in with landing gate: speech-coordinator extraction decision → M3-06 brief gate.
