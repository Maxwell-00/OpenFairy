# R0.9-01 Brief Gate — Xiaomi MiMo-V2.5-ASR cloud file worker

Gate date: 2026-07-13
Reviewer role: task-brief gate (spec fidelity + invariants). **Not** an implementation review — R0.9-01 is unbuilt.
Brief under review: `tasks/R0.9-01-mimo-v2.5-asr-cloud-file-worker.md` (ChatGPT draft, gated from the working tree pre-commit; integrity-checked — 1937 lines, no NUL/mojibake, complete tail)
Repo state: baseline `9c0ebed` ("Delay M3-06, Begin v0.9 product"; M3 archived at `d27be2d`). v0.9 plan gate: `tasks/v0.9-plan-review.md`.

Method: gated inline. The plan-gate's three MiMo clauses, the M3-06 `asr.request` approval, the coordinator carry-in text, and the M3-05 provider-hygiene regime were all verified present in the brief; the pay-as-you-go vs Token Plan endpoint/key split is the drafter's post-plan-gate research and is accepted as the brief's provider-facts baseline (the deterministic fake mirrors documented behavior; owner-live remains ground truth per standing rule).

---

## Verdict

**ACCEPTED WITH INLINE DECISIONS — three "(gate)" edits applied; otherwise dispatch-ready.**

This brief pre-answered nearly everything the gate would have demanded: the coordinator carry-in with the stop-and-report (no self-waiver) net-decrease clause; `asr.request` landed exactly as the M3-06 gate approved it (labels/credential/audio/path all absent from the wire); the base64 boundary with a conservative 7,000,000-byte raw cap AND a pre-I/O predicted-body check; the chat-dialect exception confined to worker adapter + fixtures + conformance with a protocol.md note assigned to the countersign docs pass; the secret-denial demo contract with **zero-everything evidence** (worker spawn = 0, staged bytes = 0, connections = 0, requests = 0 — denial before staging, stronger than M3-05's post-staging denial); credential-kind validation that never prints any fragment of the key; the Windows discovery flake handled with named narrow options instead of blanket retries or a global override; and the owner keyword check carrying the M2-05c `你好` escape rule unprompted.

## BLOCKER

None. **Dispatch precondition (owner):** a verified pay-as-you-go `sk-*` key that can call `mimo-v2.5-asr` — note this is NOT the Token Plan `tp-*` credential class; the brief rejects Token Plan endpoint and keys outright.

---

## Inline decisions applied ("(gate)" markers in the brief)

1. **Q8 — encoded request cap: decimal `10,000,000` for the entire predicted JSON body**, shared as one constant across worker/gateway/fake. Conservative under either MB/MiB reading given the 7,000,000 raw cap; shrinks on live evidence, never grows.
2. **Q17 — worker lifecycle: per-request**, matching M3-05 (thin HTTP client, no model load; simplest cleanup). Caching is a streaming-era optimization; interpreter-discovery caching per test process (§20) is separate and allowed.
3. Header status line updated to GATED with the dispatch precondition.

## Answers to the brief's §29 gate questions

1. `mimo-v2.5-asr-chat-http` transport name: **YES**. 2. `mimo-paygo-cn` closed profile: **YES**. 3. Pay-as-you-go only: **YES** — one credential class, one domain; Token Plan support is a future recorded decision if ever needed. 4. Runtime `sk-` shape validation: **YES** — as the named preflight (`MIMO_ASR_CREDENTIAL_KIND_MISMATCH`); it converts a confusing late auth failure into an immediate, key-free diagnosis. 5. `api-key`-only: **YES** — one auth method, closed. 6. Clearance `max_sensitivity: personal`, `residency: [region-restricted, global-ok]`, `regions: [cn]`: **YES** — balanced voice floor routes; secret/local-only audio denies; this IS the demo contract. 7. 7,000,000 raw cap: **YES**. 8. **Decided inline** (decimal 10,000,000, whole-body). 9. WAV + MP3 final: **YES** (per the plan-gate WebM ruling — the browser records WAV client-side in R0.9-02; the MIME set is closed now). 10. `audio/x-wav` rejected: **YES** — our own importer (`packages/artifacts`, WAV support added this slice under the same additive conditions as M3-05's MP3) only ever produces `audio/wav`; aliases stay out of a closed set. 11. Fixed `asr-input.bin` staging: **YES** (mirrors `tts-output.mp3`). 12. `asr.request` as approved: **YES** — confirmed against the M3-06 gate record. 13. Optional worker evidence (provider id/model/finish/usage seconds): **YES** — ack/evidence level only, never canonical (M3-06 RE-4 class). 14. Exactly one choice required: **YES** — multi-choice is malformed, fail closed, no silent first-pick. 15. Reject non-null `tool_calls`/audio output: **YES** — an ASR response bearing tool calls is out-of-contract. 16. No automatic retry/fallback despite 429/5xx retryability classification: **YES** for this slice — classification is evidence, not behavior; per-candidate at-most-once stands. 17. **Decided inline** (per-request). 18. Proposed CLI/import path as the R0.9-02 precursor: **YES** — countersign verifies it routes through the coordinator and the single voice-to-turn method. 19. `server.ts` net-decrease hard: **YES** — with the brief's own stop-and-report escape, never self-waived. 20. Windows flake hardening bounded as listed: **YES** — no blanket retries, no discovery-test deletion, narrow options only, before/after evidence required. 21. Known test interpreter for provider tests after a dedicated normal-discovery test passes: **YES** — code-gated, discovery test untouched. 22. Schema compatibility: **no canonical changes** — `speech.asr.final` exists with the exact required fields; artifact `kind: input` exists; WAV MIME is a TS-level additive addition to `packages/artifacts`. 23. Owner bilingual smoke + secret-denial: **YES, sufficient** — the secret-denial live check with the zero-everything block is the demo's core evidence; both count toward the ≥20-session S4 ledger. 24. Provider facts: **none materially different** from what this seat verified at plan gate (endpoint, api-key header, wav/mp3, ≤10 MB encoded, zh/en); the paygo/Token-Plan domain-and-key split is accepted as drafted; any owner-live discrepancy is recorded evidence, not silently patched.

---

## CARRY-IN

- **M3-05 speech-coordinator carry-in: SETTLED by this brief** (§6, hard acceptance incl. net decrease + responsibility-move table). Countersign treats the extraction as the highest-scrutiny item — first M3-05 code *move* since its close; `voice.tts-provider-v0` must stay green and unweakened.
- **M3-06 `asr.request` approval: LANDED here** (§11) — the M3-06 slice itself stays deferred; only the approved wire kind lands.
- M2 deferral landing gates: none lands at R0.9-01.

## NIT

- Suite name `voice.mimo-asr-provider-v0` is vendor-specific where prior suites were capability-named (`voice.tts-provider-v0`). Acceptable — ASR will have multiple providers eventually and per-provider conformance suites are plausible — but if Codex prefers `voice.asr-provider-v0` for symmetry, either is approved; pick one and register it.
- §2.2's "429 as retryable evidence but no retry" is exactly right for a demo sprint — resist the temptation later to add retries without a brief.
- The provider bills by audio duration (¥0.5/h): the owner check's cost is effectively zero; no budget clause needed.

---

## Reviewer-gate clauses for the delivery countersign

1. **Coordinator extraction** (highest scrutiny): M3-05 TTS orchestration moved semantically unchanged — `voice.tts-provider-v0` green/unweakened, diff-verified; no provider HTTP shape or orchestration left in `server.ts`; net line decrease vs `9c0ebed` with before/after counts + responsibility table; coordinator owns no TurnRunner/dispatch/canonical persistence; one TurnRunner; single voice-to-turn path. (§6)
2. **Zero-everything denial**: under-cleared/secret audio ⇒ worker spawn = 0, staged bytes = 0, provider connections = 0 (transport-level counting), no ASR final, no turn, no model call — asserted before staging, replayable. (§5 of plan / brief §21)
3. **Base64 confinement**: encoding exists only inside the worker at the HTTP boundary; NDJSON/JSONL/CLI/logs/diagnostics/evidence all asserted free of base64 runs; raw cap 7,000,000 + whole-body predicted cap 10,000,000 enforced pre-I/O with the turn preserved on rejection. (§11/§12)
4. **Credential hygiene**: `sk-` preflight without printing any fragment; single narrow env var; Token Plan endpoint/key rejected with named errors; closed endpoint profile; proxies excluded per M3-05 regime; poison-fixture absence sweeps across all surfaces. (§2.1/§14)
5. **Response contract**: exactly one choice; `tool_calls`/audio-output rejection; transcript extracted only from the assistant message content; max_transcript_chars enforced; provider error taxonomy incl. 429 classified-not-retried; no HTTP-200 shortcut. (§15/§16)
6. **`asr.request` fixtures** golden valid/invalid; wire free of labels/credential/audio/paths; `asr.script` untouched mock-only; wire↔canonical mapping table in the work report; emitted `speech.asr.final` uses exactly the registered fields with `audio_ref` = input artifact. (§11/§17)
7. **Windows discovery**: dedicated normal-discovery test retained and passing; floor lanes intact; any hardening within the §20 named options; before/after evidence; no production dependence on test overrides. (§20)
8. **Standing invariants**: mock/MiniMax/faster-whisper-era scans byte-identical; all prior suites green; no new canonical event types; no docs edits by Codex; no `.only`/`.skip`; tsx world; no repo-tree writes; weakening scan across all pre-existing tests; chat-dialect exception confined (grep: no chat-completions construction outside the MiMo worker adapter + fixtures).

## Dispatch instruction

Owner: obtain + verify the pay-as-you-go `sk-*` key first (the brief's own precondition), commit this brief + gate record + the v0.9 docs, then dispatch to Codex. The two things most likely to need the countersign's teeth: the coordinator extraction (first code move of sprint velocity — the weakening scan is the safety net) and the §20 flake work (the temptation to over-fix is named and fenced). With the inline decisions in: **dispatch-ready**.
