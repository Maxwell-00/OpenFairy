# R0.9-02 Final Primary Review

**Review date:** 2026-07-15  
**Accepted implementation:** `bc88889d9e30f2b16b5d304382cf1ae02a801a4f`  
**Primary-review repair CI:** `29398291297` — PASS  
**Owner-run head:** `4467b03ade6ea462c9391f51ef8ce2e134d65025`  
**Committed owner evidence:** `ab81bc936542c0b8093f4fdeba41590754fdbb2a`  
**Evidence archive head:** `9d88c74830600fb510922154f380ca1f9b3f8a92`  
**Evidence CI:** `29408324951` — PASS  
**Verdict:** **CHANGES REQUIRED — DOCUMENTATION ONLY**  
**Implementation:** **ACCEPTED**  
**Owner-live evidence:** **ACCEPTED**  
**Task status:** **OPEN pending one work-report repair**

---

## 1. Decision

The R0.9-02 implementation and owner-live evidence satisfy the technical acceptance matrix.

No implementation code change, browser-session rerun, MiMo request, MiniMax request, or new owner-live session is required.

Final close is blocked by one contradictory required deliverable:

```text
tasks/R0.9-02-work.md
```

The work report still states that the repair commit, replacement CI, and owner-live are pending. Those statements are now false and conflict with committed evidence.

The task brief explicitly requires the work report to contain:

- CI run URLs and all four job results;
- owner-live status;
- known limitations and carry-ins.

Until the report is reconciled to committed truth, R0.9-02 cannot be marked CLOSED.

---

## 2. Git-backed scope review

Compared with documentation head `4467b03`, the final evidence state adds only:

```text
tasks/owner-checks/R0.9-02/R0.9-02-owner-manual-checks-v6.md
tasks/owner-checks/R0.9-02/auth-and-ownership.json
tasks/owner-checks/R0.9-02/chrome-auto-stop.json
tasks/owner-checks/R0.9-02/chrome-manual.json
tasks/owner-checks/R0.9-02/edge-manual.json
tasks/owner-checks/R0.9-02/leak-and-residue.json
tasks/owner-checks/R0.9-02/local-preflight.txt
tasks/owner-checks/R0.9-02/owner-live-summary.json
tasks/owner-checks/R0.9-02/prerequisites.json
tasks/owner-checks/R0.9-02/voice-session-ledger.jsonl
```

The former task-root manual was moved into the evidence archive.

No implementation source, provider contract, worker, ArtifactRegistry, protocol, release plan, English specification, or `docs-zh/` file changed.

---

## 3. Owner-live acceptance

### 3.1 Chrome manual session — PASS WITH NOTE

Session:

```text
ses_01KXJH3VR4TZYQ92DJ0YYEBT4H
```

Evidence:

| Item | Result |
|---|---:|
| Browser | Chrome `150.0.7871.124` |
| Interaction | manual |
| Duration | 6,970 ms |
| WAV bytes | 223,104 |
| Samples | 111,530 |
| Format | 16 kHz / mono / PCM16 WAV |
| `speech.asr.final` | 1 |
| `turn.input` | 1 |
| local model request | 1 |
| `turn.final` | 1 |
| `speech.tts.chunk` | 1 |
| MP3 bytes | 57,012 |
| Reload/replay | PASS |
| Stored MP3 replay | PASS |
| Local Stop | PASS |
| Private provider fields visible | no |

ASR was recorded as non-empty, speech-correlated, and imperfect but usable. The owner also recorded possibly unclear articulation.

This is not a blocker. R0.9-02 validates the end-to-end Web PTT path, not ASR word-error rate.

### 3.2 Chrome exact 60-second auto-stop — PASS

Session:

```text
ses_01KXJHEMV034V20KXG097G9Z12
```

Hard-bound evidence is exact:

| Bound | Result |
|---|---:|
| Warning at 50 seconds | PASS |
| Countdown at 55 seconds | PASS |
| Auto-stop at 60 seconds | PASS |
| Duration | `60,000 ms` |
| Samples | `960,000` |
| WAV bytes | `1,920,044` |
| Later pointer release sent second turn | no |
| Recorder/media residue observed | no |
| `speech.asr.final` | 1 |
| `turn.input` | 1 |
| local model request | 1 |
| `turn.final` | 1 |
| `speech.tts.chunk` | 1 |

This closes the real-browser hard-limit acceptance item.

### 3.3 Edge manual session — PASS WITH NOTE

Session:

```text
ses_01KXJHQBD83CJMZPMZ0AAZ9BP2
```

Evidence:

| Item | Result |
|---|---:|
| Browser | Edge `150.0.4078.65` |
| Interaction | manual |
| Duration | 7,592 ms |
| WAV bytes | 242,988 |
| Samples | 121,472 |
| `speech.asr.final` | 1 |
| `turn.input` | 1 |
| local model request | 1 |
| `turn.final` | 1 |
| `speech.tts.chunk` | 1 |
| MP3 bytes | 67,380 |
| Reload/replay | PASS |
| Stored MP3 replay | PASS |
| Local Stop | PASS |
| Private provider fields visible | no |

ASR quality was again recorded as imperfect but usable and is not a release blocker for this slice.

---

## 4. Authentication and ownership — PASS

Committed keyless checks prove:

| Check | Result |
|---|---:|
| wrong-token session list | `401` |
| wrong-token upload | `401` |
| wrong-token WebSocket | close `4401` |
| wrong-token MP3 fetch | `401` |
| valid token + wrong session | `404` |
| arbitrary artifact ID | `404` |
| traversal-shaped request | `404` |
| denied bytes returned | `0` |
| model request delta | `0` |
| provider request delta | `0` |

The three speech artifacts are distinct, so the cross-session ownership result is not ambiguous under content-addressed identity.

---

## 5. Leakage and cleanup — PASS

Committed scan findings are all clean:

```text
credential leak                    false
gateway-token leak                 false
secret-ref leak                    false
Authorization/api-key header leak  false
data:audio leak                    false
long-base64 leak                   false
raw-audio marker leak              false
provider-envelope leak             false
temporary-path leak                false
artifact-path leak                 false
object-URL leak                    false
new MiMo temp roots                0
new MiniMax temp roots             0
worker-process residue             0
generated audio in evidence        0
microphone device label recorded   false
```

The final scan correctly distinguishes the harmless authentication-method value `api-key` from an actual serialized `api-key: <credential>` header.

---

## 6. Session ledger — PASS

Exactly three bounded rows are committed:

1. Chrome manual;
2. Chrome auto-stop;
3. Edge manual.

The ledger contains only the approved fields:

```text
timestamp
SID
browser/version
interaction
duration
upload bytes
ASR/turn/model/TTS counts
status
```

It contains no transcript text, audio bytes, provider request ID, credential, or local path.

The future S4 threshold remains:

```text
current accumulated rows: 3
future required rows: 20
threshold complete: false
```

R0.9-02 does not claim or waive the future 20-session gate.

---

## 7. Local deterministic evidence — PASS

The committed preflight records:

| Suite | Result |
|---|---:|
| Web pure modules | 13/13 |
| `voice.web-ptt-v0` | Cases A–N, 14/14 |
| MiMo ASR provider | 13/13 |
| MiniMax TTS provider | 13/13 |

The owner environment used Python 3.13.9 for the real local run. This is not a floor violation: the enforced minimum remains Python 3.11, and both CI floor lanes continue to validate Python 3.11 explicitly.

---

## 8. Evidence CI — PASS

Actions run:

```text
29408324951
```

checked out:

```text
9d88c74830600fb510922154f380ca1f9b3f8a92
```

and passed:

| Job | Job ID | Result |
|---|---:|---|
| `verify (ubuntu-latest)` | `87329112400` | PASS |
| `verify (windows-latest)` | `87329112381` | PASS |
| `Python 3.11 speech floor (ubuntu-latest)` | `87329112370` | PASS |
| `Python 3.11 speech floor (windows-latest)` | `87329112418` | PASS |

Both Python 3.11 lanes passed:

```text
M3-05 MiniMax TTS provider conformance
R0.9-01 MiMo ASR provider conformance
R0.9-02 Web PTT end-to-end
```

Normal Ubuntu and Windows verify jobs passed install, lint, recursive typecheck/test, and dependency rules.

---

## 9. BLOCKER

### BLOCKER 1 — required work report contradicts committed truth

`tasks/R0.9-02-work.md` currently says:

```text
Primary-review repair commit: pending
Replacement Actions: pending
Owner-live: pending / not run
Owner-live not authorized
```

Committed truth is:

```text
repair commit:
bc88889d9e30f2b16b5d304382cf1ae02a801a4f

replacement CI:
29398291297
four required jobs PASS

owner evidence archive:
9d88c74830600fb510922154f380ca1f9b3f8a92

evidence CI:
29408324951
four required jobs PASS

owner-live:
completed and accepted by final primary review
```

This violates the explicit work-report requirement and leaves the repository's principal task record internally contradictory.

### Required repair

Modify only:

```text
tasks/R0.9-02-work.md
```

Update the CI and owner-live sections to record:

- repair commit `bc88889...`;
- replacement run `29398291297`;
- its four job names, IDs, and PASS results;
- evidence commit/archive head `9d88c74...`;
- evidence run `29408324951`;
- its four job names, IDs, and PASS results;
- three owner sessions and bounded outcomes;
- exact 60-second result;
- auth/ownership PASS;
- leakage/residue PASS;
- ledger rows `3/20`;
- ASR quality note: imperfect but usable, outside this slice's quality benchmark;
- owner-live completed;
- final primary verdict remains pending until this report repair is committed.

Do not modify:

- implementation code;
- tests;
- workflow;
- provider/worker contracts;
- owner evidence JSON;
- voice-session ledger;
- plans/specs;
- `docs-zh/`.

No provider or browser rerun is required.

After the one-file repair:

1. commit and push;
2. obtain the normal four-job Actions run;
3. provide the commit SHA and run URL;
4. final re-review can close R0.9-02 without another owner-live cycle.

---

## 10. Carry-ins

### R0.9-02-C1 — content-addressed artifact identity

General label-merge/identity semantics remain the R0.9-01 carry-in. R0.9-02 correctly uses canonical session references for playback ownership.

### R0.9-02-C2 — provider attempt versus transport-observed counts

Owner evidence establishes successful real provider execution and the no-retry route contract. It records expected request counts rather than raw provider-side transport counters.

Retain the existing R0.9-01 carry-in to distinguish provider attempts from transport-observed requests in a later intentional observability slice.

### R0.9-02-C3 — ASR quality

The two short bilingual owner sessions produced non-empty but imperfect transcripts. Source articulation may also have been unclear.

Treat this as a quality-evaluation carry-in, not a functional release blocker. R0.9-02 makes no ASR accuracy or WER claim.

### R0.9-02-C4 — server line ceiling

`server.ts` remains at 1,628 non-empty lines against the `<1,629` pin. Future responsibilities should be extracted rather than compressed.

---

## 11. Current state

```text
Implementation:       ACCEPTED
Primary repair:       ACCEPTED
Replacement CI:       ACCEPTED
Owner-live:           ACCEPTED
Evidence hygiene:     ACCEPTED
Evidence CI:          ACCEPTED
Work report:          BLOCKED — stale contradictory status
R0.9-02:              OPEN
```

**Final primary verdict: CHANGES REQUIRED — DOCUMENTATION ONLY.**
