# R0.9-05′ Final Owner-Evidence Primary Review

**Evidence commit:** `d1869d20dd399af66413687f7705dee9ad142dee`  
**Owner-tested runtime commit:** `3337cbed7447e0a8755c3eeb8ef1bffd031d6dfb`  
**Evidence CI:** Actions `29562826444`  
**Verdict:** **BLOCKED — ONE DOCUMENTATION CONSISTENCY REPAIR**

## Summary

The runtime and owner-evidence gates are substantively complete.

Accepted evidence includes:

- provider-zero doctor;
- owned dev startup and cleanup;
- reused dev lifecycle and independent-gateway survival;
- one accepted normal voice session;
- one additional accidental complete voice attempt, fully disclosed;
- synthetic secret/local-only denial with zero worker/provider/model/turn activity;
- bounded MemoryGate and canonical replay scenario;
- recovered local deterministic-model endpoint mismatch with zero public/speech-provider traffic;
- three committed PNGs with SHA-256/size records and no forbidden PNG metadata chunks;
- zero committed generated audio;
- zero gateway/speech-worker residue;
- S4 ledger advanced to `4/20`;
- temporary owner mock script removed;
- four green CI jobs at the evidence commit.

## BLOCKER 1 — stale work-report status

`tasks/R0.9-05-work.md` still states that:

- the reused-launcher repair requires owner commit/push and replacement CI;
- owner checks are paused before synthetic secret denial;
- the reused lifecycle replacement CI is pending.

Those statements are false at `d1869d2`.

The task brief requires the work report to contain current CI results and owner-live status. The owner evidence commit did not update the work report.

### Required repair

Modify only:

```text
tasks/R0.9-05-work.md
```

Required changes:

1. Update the top status to record:
   - reused lifecycle repair commit `3337cbed...`;
   - repair CI `29546014727`, four jobs PASS;
   - owner evidence commit `d1869d20...`;
   - owner evidence CI `29562826444`, four jobs PASS;
   - owner checks complete;
   - final primary review pending after this report repair;
   - Fable countersign pending;
   - do not claim closed.

2. Replace stale `PENDING OWNER COMMIT/PUSH` text for the reused repair with the completed CI run and job results.

3. Add a bounded final owner-evidence section recording:
   - doctor PASS with accurate optional warnings and zero provider request;
   - owned and reused dev PASS;
   - accepted normal voice session;
   - two observed complete voice attempts total:
     - one accepted intended session;
     - one accidental complete attempt due incorrect microphone state;
   - one S4 ledger row appended, resulting in `4/20`;
   - secret denial zero-everything PASS;
   - Scenario 3 MemoryGate/replay PASS;
   - local mock endpoint mismatch recovered, with zero public/speech-provider traffic;
   - three screenshots present;
   - metadata/leak/residue scans PASS;
   - audio/live data/config/session logs/artifact registry not committed;
   - temporary owner mock script removed.

4. Record the owner-procedure deviation honestly:
   - brief allowed one short real provider voice session maximum;
   - two complete real voice attempts occurred;
   - the second was performed because the first was believed to have incorrect microphone state;
   - both were bounded and non-sensitive;
   - only the intended successful session was added to S4;
   - no further provider call is authorized;
   - deviation requires final countersign visibility but does not require rerun.

5. Keep R0.9-03+04 deferred.

## Non-blocking notes

### NOTE 1 — Scenario 3 research counts

`research_source_count`, `research_citation_count`, `tool_call_count`, and `tool_result_count` are zero. This is acceptable because the brief permits MemoryGate behavior **or** a research result with citations. The evidence records two MemoryGate decisions, canonical replay, bounded non-sensitive input, and no hidden-reasoning claim.

### NOTE 2 — screenshot review boundary

Repository evidence proves exact paths, non-empty sizes, SHA-256 values, and absence of forbidden PNG metadata chunks. The owner also recorded a visible-pixel scan. This connector review did not independently render the committed PNG pixels; final countersign may inspect them directly on GitHub.

## CI evidence

Actions `29562826444` checked out:

```text
d1869d20dd399af66413687f7705dee9ad142dee
```

All required jobs passed:

```text
verify (ubuntu-latest)
verify (windows-latest)
Python 3.11 speech floor (ubuntu-latest)
Python 3.11 speech floor (windows-latest)
```

## Current state

```text
Runtime implementation:          ACCEPTED
Owner checks:                    ACCEPTED
Owner evidence:                  ACCEPTED
Evidence CI:                     ACCEPTED
Work report:                     BLOCKED — STALE STATUS
Additional provider calls:       NOT AUTHORIZED
R0.9-05′:                        OPEN
Next action:                     DOCS-ONLY WORK-REPORT REPAIR
```
