# R0.9-05′ Work Report — Doctor, Dev Start, and Demo Package

## Status

Implementation is complete in the working tree and ready for owner commit plus primary review. This report does **not** claim R0.9-05′ closed. Required screenshot capture, four-job Actions evidence, owner-live launch/reuse/demo checks, final primary review, and countersign remain pending.

No real model, MiMo, MiniMax, or research-provider request was made during implementation or validation. No commit or push was performed.

## Baseline commit

- Exact implementation baseline/HEAD: `cb17aeaf38050aa5e18d5be737e571298e1d634e` (`start R0.9-05`).
- Brief-declared predecessor baseline: `ea1a6283dbe2e430037ef821c0aa7b45079bcc5b`.
- The working tree was clean at dispatch and is intentionally uncommitted at handoff.

## Changed-file inventory

| File | Responsibility |
|---|---|
| `.github/workflows/ci.yml` | Run `developer-preview.launch-v0` in both Python 3.11 matrix lanes. |
| `README.md` | v0.9 Developer Preview positioning, quick start, architecture, security, demo, troubleshooting, and honest deferrals. |
| `apps/cli/package.json` | Source-first package doctor alias, focused suite, and repository-owned parser dependencies. |
| `apps/cli/src/bin/fairy.ts` | Doctor/dev command dispatch, bounded usage failure, and SIGINT/SIGTERM abort wiring. |
| `apps/cli/src/dev.ts` | Shared-preflight dev launcher, ownership modes, health wait, browser open, and cleanup. |
| `apps/cli/src/doctor.ts` | Structured shared readiness registry and deterministic probes. |
| `apps/cli/src/index.ts` | Doctor/dev source-first exports. |
| `apps/cli/test/developer-preview.test.ts` | Exactly Cases A–L in `developer-preview.launch-v0`. |
| `apps/cli/test/doctor.test.ts` | Existing doctor smoke test adapted to the structured report. |
| `apps/gateway/src/speech-worker-process.ts` | Consume the shared Python candidate/version law; worker behavior otherwise unchanged. |
| `packages/voice/src/python.ts` | Provider-neutral fixed Python candidates and `>=3.11` version law. |
| `packages/voice/src/index.ts` | Source-first Python readiness exports. |
| `scripts/start-gateway.mjs` | Narrow IPC graceful-shutdown message while preserving the sole source-first gateway launcher. |
| `pnpm-lock.yaml` | Workspace importer links for existing internal packages only. |
| `examples/fairy.v0.9.yaml` | Schema-valid, secret-reference-only v0.9 example configuration. |
| `docs/demo/v0.9-demo-script.md` | Timed three-minute demo. |
| `docs/demo/scenarios/01-normal-voice.md` | Normal bilingual voice scenario. |
| `docs/demo/scenarios/02-secret-route-denial.md` | Synthetic-secret zero-provider denial scenario. |
| `docs/demo/scenarios/03-memory-research-replay.md` | Bounded memory/research/replay scenario. |
| `docs/demo/interview-project-summary.md` | 30/90-second pitches, resume bullets, differentiators, limits, and next step. |
| `docs/demo/assets/README.md` | Screenshot owner-capture and sanitization specification. |
| `tasks/R0.9-05-work.md` | This report. |

No file under `docs-zh/`, no normative English doc, no worker Python file, no protocol registry, and no artifact identity implementation changed.

## Architecture and responsibility map

| Responsibility | Owner after this slice |
|---|---|
| Stable readiness registry and report rendering | `apps/cli/src/doctor.ts` |
| Doctor CLI parsing/output | `apps/cli/src/bin/fairy.ts` + doctor exports |
| Dev preflight | the same `evaluateReadiness()` registry, consumed by `apps/cli/src/dev.ts` |
| Gateway child ownership and browser policy | `apps/cli/src/dev.ts` |
| Sole source-first gateway launch | `scripts/start-gateway.mjs` |
| Gateway runtime/listener/health/Web routes | existing gateway code, unchanged |
| Shared Python candidate and floor law | `packages/voice/src/python.ts` |
| Test/CI-only Python override read | existing speech supervisor and doctor, each gated by `NODE_ENV=test` or `CI=true` |
| Demo claims and capture rules | README plus `docs/demo/*` |

Doctor and dev do not maintain parallel readiness logic: both call the same `evaluateReadiness()` function and receive the same report plus non-serialized port/data-dir facts. Probe injection is code-only; no YAML, CLI, query-string, or new runtime environment seam was added.

## `apps/gateway/src/server.ts` byte-identity proof

| Measurement | Baseline/current |
|---|---:|
| SHA-256 | `038e7cb70578355e02366e0446513691e86f7529e3897935f6f16b21839931b3` |
| Bytes | `64,699` |
| Physical lines | `1,742` |
| Non-empty lines | `1,628` |
| `git diff -- apps/gateway/src/server.ts` | empty |

The file is byte-identical to the dispatched state and remains below the pinned `<1,629` non-empty-line ceiling. No compression or ceiling change was used.

## Doctor check registry and status semantics

The exact stable order is:

1. `runtime.node`
2. `runtime.pnpm`
3. `runtime.python`
4. `config.load`
5. `config.main-role`
6. `config.gateway-auth`
7. `config.speech-asr`
8. `config.speech-tts`
9. `storage.data-dir`
10. `storage.artifacts`
11. `storage.sessions`
12. `network.gateway-port`
13. `runtime.gateway-health`
14. `web.assets`
15. `optional.container-runtime`
16. `optional.research`

Each check is exactly `pass`, `warn`, or `fail`. `report.ok` is true iff no check fails. Warnings preserve exit code `0`; any failure produces exit code `1`. Text rendering is fixed-order and bounded. JSON rendering is one `JSON.stringify()` object with `kind: "fairy.doctor.report"`, `v: 1`, `ok`, `checks`, and `lines`, with no ANSI.

### Doctor CLI table

| Form | Result |
|---|---|
| `fairy doctor` | bounded text report |
| `fairy doctor --json` | exactly one JSON object |
| `--config <path>` | existing effective config precedence with explicit user source |
| `--data-dir <path>` | readiness override passed to dev/gateway when used by dev |
| `--port <1..65535>` | loopback-only readiness override |
| unknown, positional, missing, duplicate, invalid scalar | bounded usage, exit `1` |
| `--token`, `--api-key`, `--secret`, `--live`, `--probe-provider`, `--base-url`, `--host` | rejected |

### Synthetic JSON report example

This is an abbreviated synthetic example of the committed shape; no local path or credential value is present:

```json
{"checks":[{"id":"runtime.node","status":"pass","summary":"Node v24.0.0 satisfies >=22."},{"id":"runtime.pnpm","status":"pass","summary":"pnpm 11.7.0 is executable."},{"id":"runtime.python","status":"pass","summary":"Python 3.11.15 is ready (test-override)."},{"id":"runtime.gateway-health","status":"warn","summary":"The Fairy gateway is not running yet; the port is ready for dev start."}],"kind":"fairy.doctor.report","lines":["Fairy doctor","...","Doctor: PASS WITH WARNINGS"],"ok":true,"v":1}
```

Production JSON contains all 16 checks in the fixed order.

## Probe deadlines

| Probe/lifecycle | Code-owned bound |
|---|---:|
| pnpm / Docker / Podman command | `2,000 ms` each |
| Python discovery | `5,000 ms` total; fixed candidates receive bounded shares |
| gateway health + bind classification | `1,000 ms` |
| health response body | `8,192 bytes` |
| owned gateway startup | `15,000 ms` |
| graceful owned shutdown | `5,000 ms` |
| forced tree termination | `5,000 ms` |
| final port-release poll | `5,000 ms` |

## Secret-redaction proof

- Main, gateway, MiMo, and MiniMax readiness resolves values only to prove presence/class; values are discarded and never placed in reports.
- Gateway auth reports only secret-reference readiness or literal/default classification; it never prints, hashes, prefixes, partially reveals, or measures the token.
- MiMo output uses only the allowed label `pay-as-you-go-sk`; MiniMax output says only credential reference present/missing.
- Provider credentials and gateway tokens are absent from gateway argv, browser URL, doctor/dev output, fixtures, and demo evidence.
- Case C scans all success/failure reports against four synthetic raw secret values and `Authorization`, `api-key`, base64, and audio data-URL patterns: zero matches.
- Cases I/K assert the dev console/URL contains no secret and no token query.
- Config-error reporting exposes only bounded schema issue paths, never rejected values.

## Python discovery extraction and reuse

`packages/voice/src/python.ts` owns:

```text
Linux/macOS: python3 → python
Windows:     python3 → python → py -3
test/CI:     one FAIRY_TEST_PYTHON candidate only
floor:       strict X.Y.Z evidence, Python >=3.11
```

`SpeechWorkerProcess` calls `speechPythonCandidates(process.platform, this.#testPythonOverride)` and translates the shared version-law errors back to its existing public worker error codes. The supervisor still contains exactly one `FAIRY_TEST_PYTHON` occurrence and retains its test/CI gate. Existing production-discovery coverage was not weakened.

The explicit local floor run used the owner's `fairy-py311` conda interpreter, reported `Python 3.11.15`, executed the real doctor Python probe through the existing test-only override, and passed all 12 Cases A–L. The machine-local absolute executable path is intentionally omitted from committed evidence.

## Config, data, artifact, session, and Web checks

- Effective configuration uses the existing defaults → explicit/user → workspace → code-owned session override precedence.
- `config.main-role` reuses `parseModelGatewayConfig()` and resolves a configured credential reference without I/O.
- Speech checks reuse `parseSpeechProviderConfig()`, `resolveMimoCredential()`, and `resolveMiniMaxCredential()`; no adapter contract was copied or broadened.
- Data-dir readiness accepts a writable existing directory or writable nearest existing parent. Its random exclusive probe is always removed; cleanup failure makes the check fail.
- Artifact readiness uses `ArtifactRegistry.list()` plus a bounded raw registry integrity/count/containment check. Malformed JSONL, escaped paths, missing/non-regular targets, symlinks, unreadable state, and collisions fail without printing records, paths, hashes, or content.
- Session readiness inspects only the root type/readability, never historical JSONL content.
- Web readiness checks exactly the six gated source assets and rejects missing, directory, symlink, non-regular, or unreadable replacements.

`examples/fairy.v0.9.yaml` loaded through the real schema. `parseModelGatewayConfig()` resolved `roles.main` to `main-openai-compatible`; `parseSpeechProviderConfig()` resolved only `mimo-paygo-cn` and `cn-primary`. The file contains only `secret://` credential references; `example.invalid` is explicitly documented as unusable.

## Port and health state machine

The only network target in readiness code is `http://127.0.0.1:<validated-port>/health`.

| Health/bind result | Classification | Doctor | Dev |
|---|---|---|---|
| expected Fairy health shape | `fairy-running` | port PASS, health PASS | Mode B, reuse |
| health absent + exclusive loopback bind succeeds | `available` | port PASS, health WARN | Mode A, start |
| invalid/unhealthy health + bind unavailable | `occupied` | port FAIL, health FAIL | Mode C, no spawn/open |

Health reads are bounded and redirects are rejected. There is no host/base-URL option and no arbitrary endpoint probe.

## Dev CLI and source-first spawn vector

### Dev CLI table

| Form | Result |
|---|---|
| `fairy dev` | shared preflight, own/reuse gateway, open Web after health |
| `--config <path>` | validated config forwarded |
| `--data-dir <path>` | validated effective data root forwarded |
| `--port <1..65535>` | validated loopback port forwarded |
| `--no-open` | skip browser process only |
| unknown/duplicate/missing/positional/forbidden | bounded usage or preflight failure, zero spawn/open |

The sole runtime chain is:

```text
pnpm fairy dev
→ node scripts/run-cli.mjs dev
→ node --import tsx apps/cli/src/bin/fairy.ts dev
→ apps/cli/src/dev.ts
→ node scripts/start-gateway.mjs [validated config/data-dir/port only]
→ node --import tsx apps/gateway/src/bin/gateway.ts
```

Every spawn uses `shell:false`. There is no build, `dist`, sibling-package build, second launcher, second listener, remote bind, or credential in argv.

## Gateway ownership modes

| Mode | Behavior |
|---|---|
| A — available | spawn the existing source-first launcher, mark owned, poll bounded `/health`, then open/print `/web/` |
| B — healthy Fairy | spawn no gateway, print `Gateway: REUSED`, wait for launcher stop, never signal the existing gateway |
| C — occupied/unhealthy/non-Fairy | shared preflight fails; gateway spawns `0`, browser opens `0` |

The browser URL is constructed only as `http://127.0.0.1:<port>/web/`; it has no token. Windows opens through fixed `cmd.exe /d /s /c start "" <url>`, macOS through `open`, and Linux through `xdg-open`; all use `shell:false` and no user-selected executable. Browser-open failure is printed as WARN while the healthy gateway remains alive.

## Ctrl+C and cleanup state machine

1. CLI converts either `SIGINT` or `SIGTERM` to an abort signal.
2. Reused mode exits the launcher only and sends no process signal.
3. Owned mode sends the fixed IPC `fairy.launcher.shutdown` message.
4. The existing launcher forwards graceful `SIGTERM` to its gateway child.
5. Dev waits `5,000 ms`; if needed, Windows runs fixed `taskkill.exe /pid <owned-pid> /t /f` (Unix uses `SIGKILL`).
6. Dev waits for exit and polls until the selected loopback port is free.

Cases I and K executed real source-first gateway children on Windows. Both ended with gateway result `started`, port release `true`, and a subsequent classification of `available`. Case K proved the gateway was healthy when synthetic browser opening failed, then stopped cleanly after the injected SIGTERM-equivalent abort. The launcher creates no temporary file; data-root probes left zero `.fairy-doctor-*` residue.

## Preflight and provider-network zero proof

Case G made provider-shaped loopback sentinels available and ran doctor plus failed/successful dev preflights. Counters were exactly:

```text
model connections       0
MiMo connections        0
MiniMax connections     0
research connections    0
gateway child spawns    0 (failed/reused preflight paths)
browser opens           0
sessions created        0
artifacts created       0
turns submitted         0
```

Case H independently proved a required credential failure produced gateway child `0`, browser open `0`, bounded remediation, and no `.fairy*` probe residue. Doctor/dev contain no provider request implementation; their sole `fetch` is the fixed loopback health target.

## README and demo inventory

- README contains all required sections, Windows PowerShell quick start, Windows config/data locations, exact precedence, Mermaid architecture, Web walkthrough, security summary, troubleshooting, and repository document map.
- Claims are bounded: **OpenFairy v0.9 Developer Preview**, voice ledger **3/20**, R0.9-03+04 deferred, M3-06 deferred, local Stop is not barge-in, and ASR accuracy is not benchmarked here.
- The timed script covers `00:00–03:00` in the five required segments.
- Scenario 2 explicitly uses a synthetic fixture and never owner personal data.
- Scenario 3 uses only existing memory/research/replay commands and claims no hidden reasoning.
- Interview summary includes 30/90-second pitches, five resume bullets, stack, differentiators, two limitations, and next step.

## Screenshot owner-gate status

**PENDING OWNER CAPTURE — not fabricated.** The three required PNGs do not yet exist:

```text
docs/demo/assets/01-doctor-pass.png
docs/demo/assets/02-web-voice-roundtrip.png
docs/demo/assets/03-replay-governance.png
```

README references are intentionally present ahead of capture. `docs/demo/assets/README.md` contains the exact pixel/metadata sanitization rules. No PNG, GIF, audio, live data, owner config, session JSONL, or registry was generated or committed by this work.

## Cases A–L results

`pnpm --filter @fairy/cli test:developer-preview`:

```text
Test Files  1 passed (1)
Tests       12 passed (12)
Suite       developer-preview.launch-v0
Cases       exactly A, B, C, D, E, F, G, H, I, J, K, L
```

| Case | Result | Discriminating evidence |
|---|---|---|
| A | PASS | kind/version/order/status/ok/text/JSON/ANSI |
| B | PASS | allowed and forbidden parse matrix, duplicate/missing/invalid, exit law |
| C | PASS | model/auth/MiMo/MiniMax presence/class and no-secret scans |
| D | PASS | shared candidates/floor, invalid/old rejection, test-only override, supervisor source proof |
| E | PASS | writable/collision/cleanup, malformed artifacts, session collision, exact asset replacements |
| F | PASS | real available/Fairy/occupied loopback states and deadline |
| G | PASS | four provider counters zero plus no sessions/artifacts/turns |
| H | PASS | required failure gives zero gateway/browser and no residue |
| I | PASS | real source-first owned gateway, `/web/`, open-after-health, safe output, port release |
| J | PASS | reused gateway survives launcher stop; occupied service blocks spawn/open |
| K | PASS | real Windows owned cleanup, browser failure WARN, IPC/tree fallback source, port release |
| L | PASS | server byte pin, example/README/demo/deferred/dependency/protected-tree policy |

## Local validation results

| Command | Result |
|---|---|
| `pnpm install` | PASS — all 17 workspace projects, lockfile already up to date |
| `pnpm lint` | PASS — encoding guard scanned 253 files; ESLint zero warnings/errors |
| `pnpm -r typecheck` | PASS — all 16 script-bearing workspace projects |
| `pnpm --filter @fairy/cli test:developer-preview` | PASS — 1 file, 12/12 |
| `pnpm --filter @fairy/cli test -- --reporter=verbose` | PASS — 10 files, 33/33 |
| `pnpm --filter @fairy/testing test:voice-web-ptt` | PASS — `voice.web-ptt-v0`, 14/14 |
| `pnpm --filter @fairy/testing test:voice-mimo-asr-provider` | PASS — `voice.mimo-asr-provider-v0`, 13/13 |
| `pnpm --filter @fairy/testing test:voice-tts-provider` | PASS — `voice.tts-provider-v0`, 13/13 |
| `pnpm --filter @fairy/testing test:voice-worker-process` | PASS — `voice.worker-process-v0`, 4/4 |
| `pnpm -r test` | PASS — all packages; `@fairy/testing` 15 files passed + 1 intentional deferred file, 123 passed + 1 skipped; CLI 33/33 |
| `pnpm dep-check` | PASS — 129 modules / 510 dependencies, zero violations |
| `pnpm conformance` | PASS — mock mode, 18/18 cases |
| `git diff --check` | PASS |

### Explicit Python 3.11 result

```powershell
$env:NODE_ENV = 'test'
$env:FAIRY_TEST_PYTHON = '<EXACT_PYTHON_3_11_EXECUTABLE>'
pnpm --filter @fairy/cli test:developer-preview
```

Result: `Python 3.11.15`; 1 test file passed; 12 tests passed; Case D exercised the real doctor probe with source `test-override`. Both variables were removed afterward.

## CI status and workflow evidence

The workflow still has the required four jobs:

```text
verify (ubuntu-latest)
verify (windows-latest)
Python 3.11 speech floor (ubuntu-latest)
Python 3.11 speech floor (windows-latest)
```

The Python matrix now adds one focused step named `R0.9-05 doctor/dev readiness on Python 3.11`, retains `FAIRY_TEST_PYTHON: ${{ steps.python.outputs.python-path }}`, and runs only `pnpm --filter @fairy/cli test:developer-preview`. Existing MiMo, MiniMax, and Web PTT floor steps are unchanged.

Actions URL/job results: **PENDING OWNER COMMIT/PUSH**. The task expressly forbids Codex from committing or pushing, so no truthful replacement Actions run exists yet.

## Owner-live status and proposed owner-check procedure

Owner-live: **PENDING / NOT RUN / NOT YET AUTHORIZED BY PRIMARY REVIEW**.

After the owner commits this working tree, obtains four green CI jobs, and receives primary-review approval:

1. Open a fresh Windows PowerShell 7 terminal. Load the real values only into process environment variables; do not print them or capture an environment dump.
2. Run `pnpm install --frozen-lockfile`, then:

   ```powershell
   pnpm fairy doctor
   pnpm fairy doctor --json
   ```

   Save only sanitized bounded text/JSON. Confirm required PASS, honest optional WARN, one JSON object, and zero provider traffic.
3. Run `pnpm fairy dev`. Confirm the printed health and Web URLs are loopback, `/web/` contains no token, and the browser opens only after health. Enter the token only in the in-memory Web field.
4. Execute scenario 1 once with the documented non-sensitive bilingual phrase. If a successful real voice call occurs, do not repeat it merely for screenshots. Do not describe the transcript as an accuracy benchmark.
5. Press local Stop once and describe it only as local playback stop. Reload/replay the session.
6. Press Ctrl+C. Confirm the owned gateway process count is `0`, the selected port is free, and launcher temp residue is `0`.
7. Test Mode B source-first in a separate terminal:

   ```powershell
   pnpm --filter @fairy/gateway start -- --config <approved-config-path> --data-dir <approved-data-dir> --port <approved-port>
   ```

   In the demo terminal run `pnpm fairy dev --config <same-path> --data-dir <same-dir> --port <same-port> --no-open`, observe `Gateway: REUSED`, stop only dev, and prove the original gateway still answers `/health`. Then stop the original gateway normally.
8. Execute scenario 2 with a locally prepared **synthetic secret fixture, never owner personal data**. Confirm worker/staged/provider/final/turn/model counts are all zero. Do not commit the audio.
9. Execute scenario 3 with bounded non-sensitive data and capture canonical replay/tool/governance evidence.
10. Capture the three required PNGs at the exact paths, then apply the pixel and metadata scan in `docs/demo/assets/README.md`.
11. Under `tasks/owner-checks/R0.9-05/`, commit only bounded sanitized text/JSON evidence. Scan it, README/demo files, PNG metadata, and bounded replay summaries for credentials, gateway token, header values, provider bodies/request IDs, absolute paths, audio/base64/data URLs, query tokens, microphone labels, and personal transcripts. Expected count: zero.
12. Record the implementation commit, Actions URL and all four job IDs/results, owner-live result, screenshot hashes, cleanup result, and any single newly successful voice-session ledger row before requesting final primary review/countersign.

## Known limitations, deviations, and conflicts

- No implementation conflict required `server.ts`; it remained byte-identical.
- No functional deviation from the gated brief is known.
- Screenshot assets, Actions evidence, owner-live evidence, and final review are intentionally pending.
- The example text-model endpoint is intentionally unusable until the owner replaces the placeholder.
- Doctor cannot establish provider service availability by design; it validates only configuration and credential presence/class and performs zero provider I/O.
- Browser auto-open success proves process launch, not a specific browser brand; owner capture remains the final UI evidence.
- R0.9-03+04, local ASR, streaming, VAD, endpointing, barge-in, installer/service/tray, and autonomous workflows remain deferred.

Unresolved conflicts: none.

## Carry-ins

- R0.9-02 remains accepted/closed; its voice-session ledger carries forward as `3/20`.
- R0.9-03+04 remains `DEFERRED / NOT DISPATCHABLE`; no runtime symbol or implementation from it was added.
- M3-06 faster-whisper local ASR remains deferred to full M3/v1.0.
- Existing one TurnRunner, voice-to-turn path, provider contracts, artifact law, canonical registry, and normal Windows interpreter-discovery coverage remain intact.

## Proposed reviewer-owned normative-doc edits

No normative English document was edited. During the reviewer-owned documentation pass after evidence/closure:

1. add `developer-preview.launch-v0` and its Python 3.11 floor coverage to `docs/specs/evals.md`;
2. mark the R0.9-05′ doctor/dev/demo deliverables and screenshot/owner evidence in `docs/ROADMAP.md` and the v0.9 final plan;
3. add a concise source-first doctor/dev ownership note to `docs/ARCHITECTURE.md` if the reviewer wants the new CLI responsibility boundary reflected normatively.

These are proposals only. No English normative doc or `docs-zh/` file was changed.
