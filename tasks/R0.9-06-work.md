# R0.9-06′ work report — release gate and deferral ledger

## 1. Baseline commit

- Exact implementation starting commit: `9b2968f0ea1e11cd1868c6bf28e04c6e711c775a`.
- Historical release baseline retained inside the task and release records: `e39a4d672b29bec55d01b527abc59dca9b062363`.
- Starting branch: `main`, aligned with `origin/main`; starting working tree was clean.
- This report describes an uncommitted working tree. No commit, push, tag, package publication, or GitHub Release was performed.

## 2. Gated decisions D1–D4

| Decision | Implementation evidence |
|---|---|
| D1 — one plan | The binding 60-second contract is in `docs/v0.9/OpenFairy-v0.9-final-tiered-plan.md`; provenance is explicit; the temporary amendment is deleted. |
| D2 — screenshots | All three PNGs are byte-identical; `docs/demo/assets/README.md` records the accepted path-line deviation and public-derivative crop guidance. |
| D3 — identity | External name is exactly **OpenFairy v0.9 Developer Preview**; internal name is exactly **Portfolio Release Track**; versions/tags/releases are unchanged. |
| D4 — Tier 2 | R0.9-03+04 remains **DEFERRED / NOT DISPATCHABLE** and M3-06 remains gated-but-deferred at its pinned model revision. |

## 3. Changed-file inventory

Expected working-tree inventory:

```text
README.md
apps/cli/test/developer-preview.test.ts
docs/ARCHITECTURE.md
docs/PRD.md
docs/ROADMAP.md
docs/demo/assets/README.md
docs/demo/v0.9-demo-script.md
docs/v0.9-deferrals.md                                      [new]
docs/v0.9/OpenFairy-v0.9-final-tiered-plan.md
docs/v0.9/OpenFairy-v0.9-final-tiered-plan-60s-amended.md   [deleted]
tasks/R0.9-06-work.md                                       [new]
```

There are no files outside the brief's expected scope.

## 4. Release/docs policy, not runtime

The slice changes release governance, bounded current-state documentation, and deterministic Case L policy only. It adds no runtime capability, provider route, event family, endpoint, dependency, package version, manifest, workflow, or CI change. Production runtime source is untouched.

## 5. Canonical-plan merge procedure

The two committed plan blobs were compared directly:

```powershell
$Canonical = git rev-parse 'HEAD:docs/v0.9/OpenFairy-v0.9-final-tiered-plan.md'
$Amended   = git rev-parse 'HEAD:docs/v0.9/OpenFairy-v0.9-final-tiered-plan-60s-amended.md'
git diff $Canonical $Amended --unified=0 --
```

All non-metadata amendment additions were extracted and checked against the consolidated plan. The comparison found 50 contract lines: 49 byte-exact and one unit-spelling normalization, `60 s` → the gated brief's exact `60 seconds`. With that normalization the result is 50/50 present and zero missing requirements. No closed R0.9-02 review record was edited.

## 6. 60-second contract proof

The canonical plan now includes:

```text
getUserMedia + AudioContext PCM capture
→ resample to 16 kHz mono PCM16
→ encode complete WAV in browser
→ stop / auto-stop at 60 seconds
→ authenticated complete-file upload
```

It also fixes recommended duration `≤30 seconds`, warning at 50 seconds, visible countdown at 55 seconds, hard stop at 60 seconds, `max_samples = 960000`, and `max_wav_bytes = 1920044`. Sample count is authoritative; the limits are repository-owned and not configurable through YAML, CLI, query, or browser override. The plan repeats pre-registration/pre-provider fail-closed zeros for an over-limit constructed upload.

## 7. Deleted-amendment rationale and provenance

The canonical plan records the sequence: original 90-second plan → binding R0.9-02 gate D1 60-second amendment → R0.9-06′ consolidation. The temporary amendment file was deleted only after parity checking. Git history and closed review records preserve the historical reference; those records were not rewritten.

## 8. Release naming

```text
external: OpenFairy v0.9 Developer Preview
internal: Portfolio Release Track
```

This is documentation identity only. The repository does not claim production readiness, complete M3/M4/M5, or v1.0, and no tag or GitHub Release was created.

## 9. Current ledger derivation

`tasks/owner-checks/R0.9-02/voice-session-ledger.jsonl` contains exactly four non-empty committed rows. No row was added. Therefore the current release ledger is `4/20`, and S4 is incomplete. Fixture sessions and the accidental second R0.9-05′ attempt do not create additional accepted rows.

## 10. Historical 3/20 versus current 4/20

R0.9-02 closed at `3/20`. R0.9-05′ contributed one accepted owner session, making the current committed count `4/20`. README and the demo script now state both facts without rewriting the historical close.

## 11. Complete stable-ID deferral matrix

The authoritative full-field rows are in `docs/v0.9-deferrals.md`; every row includes source, status, reason, user impact, security/trust impact, current evidence, owner, landing gate, expiry condition, and explicit waiver rule.

| Stable ID | Named landing gate |
|---|---|
| `M2-S4-20` | M4 entry gate |
| `M2-PERSONA-JUDGE-90` | M4 exit gate or first critic-role wiring, whichever comes first |
| `M2-VECTOR-200K` | Brief gate of first vector-implementation task |
| `M2-MEMORY-CANARY-CONTRADICTION` | Model-backed consolidation task in M4 |
| `M2-GOVERNANCE-FRICTION-SOAK` | M5 soak |
| `M2-REAL-VISION-OCR` | Separately gated real perception-provider task |
| `M2-LEARNED-SKILL-ACTIVATION` | Separately gated learned-skill activation task |
| `R09-01-ARTIFACT-IDENTITY` | First neutral artifact-store identity/registry task |
| `R09-01-PROVIDER-OBSERVABILITY` | First provider-observability counter task |
| `R09-01-SHUTDOWN-CANCEL-CLASSIFICATION` | First speech-lifecycle shutdown/cancel classification task |
| `R09-02-SERVER-TS-EXTRACTION-FIRST` | Brief gate of any slice touching `server.ts` |
| `M3-06-LOCAL-ASR` | Full M3/v1.0 M3-06 dispatch gate |
| `M3-STREAMING-FRAMING` | M3 streaming-framing protocol gate |
| `M3-VAD-ENDPOINTING` | M3 VAD/endpointing gate |
| `M3-STREAMING-ASR-TTS` | M3 streaming provider gate |
| `M3-LANE-A-B` | M3 Lane A/B turn-integration gate |
| `M3-ACK-BANK` | M3 acknowledgement-bank gate |
| `M3-BARGE-IN` | M3 barge-in cascade gate |
| `M3-DESKTOP-TRAY` | M3 desktop-client gate |
| `M3-LATENCY-WER-CER` | M3 exit benchmark gate |
| `R09-03+04-MORNING-BRIEFING` | Fresh brief/full-depth gate after S4 ≥20 or explicit waiver |
| `M4-SUBAGENTS` | M4/SA-01 subagent contract gate |
| `M4-BOUNDED-FANOUT` | M4/SA-03 bounded fan-out gate |
| `M4-GENERIC-WORKFLOW` | M4 generic-workflow runtime gate |
| `M4-TELEGRAM` | M4 first-IM-client gate |
| `M4-UNATTENDED-7D` | M4 exit seven-day soak gate |
| `M5-INSTALLER` | M5 installer/packaging gate |
| `M5-MCP` | M5 MCP extension-surface gate |
| `M5-SKILLS-HOOKS` | M5 skills/hooks extension-surface gate |
| `M5-DASHBOARD` | M5 dashboard/privacy gate |
| `M5-COMPLETE-HARDENING` | M5 complete-hardening gate |
| `M5-EXTENSION-DOCS` | M5 extension freeze/documentation gate |
| `M5-SOAK-2W` | M5 exit two-week soak gate |
| `M5-V1` | M5 final exit and v1.0 release gate |

The ledger contains no `TBD`; every active row ends with an explicit decision requirement and “silence is not a waiver.”

## 12. Accepted deviations register

- `DEV-R09-05-SECOND-VOICE-ATTEMPT`: two bounded complete attempts, one intended accepted session, one accidental microphone-state attempt, one S4 row, accepted without rerun or landing task.
- `DEV-R09-05-SCREENSHOT-PATH`: screenshot 2 clean; screenshots 1 and 3 retain the benign repository-path prompt line; canonical evidence stays unchanged and public derivatives should crop it.
- `NOTE-R09-02-ASR-QUALITY`: transcripts are non-empty but imperfect; no WER/CER or ASR-accuracy benchmark claim; M3 owns that benchmark.

## 13. ROADMAP overlay

The delimited `Current release overlay — OpenFairy v0.9 Developer Preview` says R0.9 is an overlay, R0.9-01/02/05′ are closed, R0.9-06′ remains in review, S4 is 4/20 incomplete, and M3/M4/M5 exits remain open. Existing milestone criteria are byte-untouched outside the inserted overlay.

## 14. PRD overlay

The delimited `Current implementation overlay — v0.9 Developer Preview` separates implemented text/session/replay, governance, memory/research, sandbox/tooling, non-streaming speech, authenticated Web, doctor/dev, and demo scenarios from target-state voice, client, orchestration, packaging, hardening, and v1 metrics.

## 15. ARCHITECTURE overlay

The delimited `Current v0.9 topology overlay` records the complete browser-WAV-artifact-MiMo-TurnRunner-MiniMax-MP3-replay path, current CLI/Web clients, source-first TypeScript, single gateway ownership, browser projection discipline, provider dialect containment, and raw/base64 exclusion from NDJSON/JSONL. Target diagrams remain unchanged.

## 16. README/demo reconciliation

README links the canonical plan and authoritative deferral ledger, distinguishes historical 3/20 from current 4/20, and keeps S4 incomplete. The three-minute demo closing segment states the same derivation. Provider-consent/cleanup instructions and all three scenario boundaries remain unchanged.

## 17. Screenshot/hash preservation

```text
01-doctor-pass.png          d4a789dd8eea29078d8f3b0cf6846bbea4fe047172876ea8a6a54c7d98afe438
02-web-voice-roundtrip.png 87a22ce56e5ff2017e7b4d80177dc175e18261bfcbcc5f65d562b415fa324c4c
03-replay-governance.png   7f504e2623b6699941ef638cb5d5dd8ff4981794b58f55095c86b7336d2666e8
```

All three paths remain exact and the PNG diff is empty. No OCR, edit, crop, recapture, or metadata rewrite was performed.

## 18. Case L before/after responsibility

The suite remains `developer-preview.launch-v0` with exactly Cases A–L and all preceding assertions. Case L now enforces the 17 release-policy clauses: 4/20 truth; no 20/20/S4 completion; 3/20 versus 4/20; all stable IDs; full fields/landing gates/no-silent-waiver; three overlays; canonical 60-second contract; amendment absence; screenshot completion/deviation; exact PNG paths/hashes; no production/full-milestone/v1 promotion; R0.9-03+04 deferral; M3-06 status/revision; four-part server pin; and no release/workflow/subagent behavior in sampled production runtime surfaces.

## 19. Exact local validations

| Command | Result |
|---|---|
| `pnpm install` | PASS — all 17 workspace projects, lockfile already current, pnpm 11.7.0 |
| `pnpm lint` | PASS — encoding guard 253 files; ESLint zero warnings |
| `pnpm -r typecheck` | PASS — 16 of 17 workspace projects |
| `pnpm --filter @fairy/cli test:developer-preview` | PASS — 1 file, 12/12 tests, exactly Cases A–L |
| `pnpm --filter @fairy/cli test -- --reporter=verbose` | PASS — 10 files, 33/33 tests |
| `pnpm -r test` | PASS — 16 of 17 workspace projects; `@fairy/testing` 15 files passed + 1 intentional skip, 123 passed + 1 skipped; CLI 10 files/33 tests; all other package suites passed |
| `pnpm dep-check` | PASS — 130 modules, 512 dependencies, zero violations |
| `pnpm conformance` | PASS — mock mode, 18/18 cases |
| `git diff --check` | PASS — no whitespace errors |

## 20. Four-job CI status

Replacement CI is not available because this working tree has not been committed or pushed, as required by the task. Required post-push jobs remain: verify Ubuntu, verify Windows, Python 3.11 speech floor Ubuntu, and Python 3.11 speech floor Windows.

## 21. No-provider proof

Provider request count is `0`. No owner-live flags were set, no browser voice was run, and no MiMo, MiniMax, model, research, or other public-provider request was made. Validation is deterministic and keyless.

## 22. No-runtime-diff proof

Final diff audits compare the working tree with `9b2968f...`. Results:

| Protected scope | Changed paths |
|---|---:|
| `apps/gateway/src apps/cli/src apps/web packages workers scripts/start-gateway.mjs scripts/run-cli.mjs` | 0 |
| `apps/gateway/src/server.ts` | 0 |
| `tasks/owner-checks/**` | 0 |
| `tasks/owner-checks/R0.9-02/voice-session-ledger.jsonl` | 0 |
| `docs/demo/assets/*.png` | 0 |
| `docs-zh/**` | 0 |

Only `apps/cli/test/developer-preview.test.ts` changes under application code. `git diff --name-only 9b2968f...HEAD` is empty because HEAD remains the exact starting commit; all deliverables remain intentionally uncommitted.

## 23. `server.ts` byte identity

```text
blob:             7fe2116db5c9ad95f7ea853c06d7b149ebeab4c2
SHA-256:          038e7cb70578355e02366e0446513691e86f7529e3897935f6f16b21839931b3
bytes:            64,699
non-empty lines:  1,628
ceiling:          <1,629
```

Case L pins all four identity values. The file diff is empty.

## 24. `docs-zh/` untouched

`docs-zh/` was not read, edited, recreated, or translated. The final scoped diff audit is empty.

## 25. Owner-check status

The R0.9-06′ owner release check is not authorized at this stage and was not performed. It starts only after owner commit/push, four green CI jobs, primary review, and approval of a bounded procedure. No owner evidence file was created or changed.

## 26. Known limitations

- S4 is 4/20 and incomplete.
- ASR evidence is functional, non-empty, and imperfect; it is not an accuracy benchmark.
- Voice is non-streaming complete-file cloud ASR/TTS; local ASR, VAD, partials, Lane A/B, acknowledgement bank, and barge-in are absent.
- The preview has no tray/IM client, workflow/scheduler, subagent runtime, installer, production-hardening claim, or v1.0 claim.
- Replacement CI and owner release evidence require owner-controlled post-implementation stages.

## 27. Carry-ins

All 34 active stable-ID rows in §11 remain binding until their named gates. Highest-near-term controls are S4 ≥20 before M4 entry, extraction-before-addition for any `server.ts` touch, neutral artifact identity semantics, provider count semantics, shutdown cancellation classification, and the still-deferred M3-06 model gate.

## 28. Proposed countersign/docs actions

After the owner commits/pushes this implementation and all four jobs are green: primary review should diff-check runtime zero, overlay-only normative edits, D1 parity/provenance/deletion, the complete ledger, Case L, PNG hashes, and release claims. The owner may then execute the separately approved provider-zero release observation and commit its bounded JSON summary; an evidence CI and final Fable countersign follow. No normative subsystem-spec edit is proposed by this slice. Neither R0.9-06′ nor Tier 1 is self-closed here.
