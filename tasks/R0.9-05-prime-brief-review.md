# R0.9-05′ Brief Gate — Doctor, One-Command Dev Start, Interview Demo Package

Gate date: 2026-07-15
Reviewer role: task-brief gate (light tier per the v0.9 plan — this slice touches no trust surface).
Brief under review: `tasks/R0.9-05-prime-doctor-dev-start-demo-package.md` (committed at `27fb0d6`; integrity-checked — 1552 lines, no NUL)
Repo state: R0.9-02 closed at `ea1a628`. The R0.9-03+04 deferral record (`tasks/R0.9-03+04-DEFERRED-bounded-morning-briefing.md`) is acknowledged as a well-formed non-dispatchable deferral — no gate action required on it now; its ledger entry lands at R0.9-06′.

---

## Verdict

**ACCEPTED — status edit only; zero substantive patches. Dispatch-ready.**

Every clause this gate would have written is already in the brief, most of them stricter than required:

- **The `server.ts` landing gate is carried verbatim** (§4.1: DO NOT MODIFY, with the full stop-and-report protocol — no compression, no self-raised ceiling; the slice expects zero changes).
- **Readiness tooling generates zero provider traffic** (§4.3) and doctor/dev share one check registry so they cannot diverge (§6.2).
- **Token hygiene exceeds the ask**: doctor "never print, hash, partially reveal, measure, or echo the token" (:376); credentials are presence/class only; forbidden flags (`--token`/`--api-key`/`--live`/`--base-url`/`--host`) rejected at parse.
- **Gateway ownership semantics are honest** (Modes A/B/C): a reused gateway is never killed on Ctrl+C; an owned child gets graceful-then-forced shutdown with zero orphans/temp residue; Windows behavior mandatory; health polling only against `127.0.0.1:<port>/health` with a code-owned 15 s deadline; browser-open failure is WARN, never a reason to kill a healthy gateway.
- **The demo package is the deliverable that matters** and it is specified with timing (3-minute script, five segments), three scenarios that use only existing capabilities (scenario 2 is the zero-everything denial — the demo's crown jewel — with "do not invent a new runtime feature" stated), an 11-item screenshot forbidden list with an owner sanitization gate, and explicit honesty rules (no ASR-benchmark claims; local Stop named as local; deferrals shown in the script's closing segment).
- **Example config** uses `secret://` refs + `example.invalid` placeholders only, loads under the real schema, and pins the closed MiMo/MiniMax contracts.
- Suite `developer-preview.launch-v0` (Cases A–L) starts a new non-voice naming family — appropriate. CI stays four keyless jobs.
- Even this session's mount-staleness incident made it into the brief's read-first rules (working-tree views are low-trust vs the committed tree).

## Gate notes (no patches)

1. The demo script's closing segment ("architecture and honest deferrals") should name the version as **Developer Preview** and keep the S4 3/20 ledger honest if asked — this is already implied by the plan's naming ruling; countersign will check the delivered script text.
2. Scenario 2 must use a synthetic secret fixture, never owner personal data — the brief's "do not expose a real credential or provider body" covers it; the delivered scenario doc should say "synthetic" explicitly.
3. `pnpm fairy dev` spawning `scripts/start-gateway.mjs` keeps the tsx single-world rule by construction; the countersign greps for any plain-node spawn anyway.

## Reviewer-gate clauses for the delivery countersign (light tier)

1. `apps/gateway/src/server.ts` byte-identical to `ea1a628`; ceiling tests untouched.
2. Doctor/dev share one registry; no provider/network calls from either (grep fetch/http targets — loopback health only); forbidden flags rejected with tests.
3. Token/secret sweep: no token/credential material in doctor output (text or JSON), dev console, argv, or the example config; JSON mode is exactly one object, no ANSI.
4. Ownership modes A/B/C each tested; Ctrl+C on reused gateway leaves it alive (test); owned-child shutdown leaves no orphan/port/temp residue.
5. Demo package files present with the honesty lines (Developer Preview naming, local-stop wording, synthetic-secret note, no benchmark claims); screenshots deferred to the owner gate as specified.
6. Standing invariants: suites all green and untouched; suite `developer-preview.launch-v0` the only new name; no new listeners/canonical types; no `.only`/`.skip`; weakening scan.

## Dispatch instruction

Owner: commit this gate record, dispatch to Codex. This is the slice where the sprint's value gets packaged for its actual audience — the three scenario docs and the 3-minute script deserve as much of Codex's care as any invariant. R0.9-03+04 stays untouched per your instruction.
