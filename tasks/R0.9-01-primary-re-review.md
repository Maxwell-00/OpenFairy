# R0.9-01 Primary Re-review Final — Xiaomi MiMo-V2.5-ASR

**Review date:** 2026-07-14  
**Reviewed committed state:** `d63ae074cb377f5f290b1d4a175feb3d1c702207`  
**Cancellation repair commit:** `8e211755c195d2a06aeacdbc1745e8f209b7d5c3`  
**Evidence commit:** `d63ae074cb377f5f290b1d4a175feb3d1c702207`  
**Implementation Actions:** `29337325296`  
**Final-head Actions:** `29338293955`

## Verdict

**ACCEPTED FOR OWNER-LIVE / OPEN**

The remaining cancel-before-final blocker is closed.

R0.9-01 is not closed. Required remaining evidence:

1. owner real MiMo pay-as-you-go ASR check;
2. owner synthetic secret/local-only denial check;
3. committed bounded owner evidence;
4. final primary close;
5. Fable/Opus countersign;
6. reviewer-owned English docs pass.

## Blocker closure

### Immediate pre-worker cancellation

The gateway reservation now includes cancellation state:

```ts
{ kind, token, cancelled }
```

`turn.cancel` marks the reservation before calling both TurnRunner and coordinator cancellation. The cancel ACK therefore does not depend on a worker already existing.

A code-gated barrier pauses after session reservation and before coordinator execution. The deterministic test proves:

```text
cancel ACK             true
worker spawn           0
staged bytes           0
provider connection    0
provider request       0
speech.asr.final       0
turn.input             0
model request          0
provider request bytes 0
temporary residue      0
```

### Provider-complete / pre-canonical cancellation

A second code-gated barrier pauses after a valid provider result but before canonical `speech.asr.final`.

The deterministic test proves:

```text
cancel ACK             true
worker spawn           1
staged bytes           16
provider connection    1
provider request       1
speech.asr.final       0
turn.input             0
model request          0
transcript exposed     no
temporary residue      0
```

### Coordinator lifecycle

The cancellation callback is checked across the bounded ASR lifecycle:

- after artifact validation;
- before staging;
- after staging and before worker construction;
- after worker startup and before provider request;
- after provider completion;
- after worker shutdown/temp cleanup and before returning an accepted transcript.

Existing workers are cleaned through the existing bounded `finally` path.

### Canonical boundary

`server.ts` checks exact reservation ownership and cancellation state immediately before canonical final emission. A provider transcript returned after cancellation is discarded.

### Reservation ownership

Only the exact reservation token or active coordinator object may clear its map entry. An older operation cannot delete a later reservation.

## Test seam

The two barriers are TypeScript constructor-only test hooks:

- `beforeAsrCoordinator`;
- `beforeAsrCanonicalFinal`.

They are rejected unless `NODE_ENV=test` or `CI=true`.

They are not available through:

- YAML;
- CLI;
- configuration schema;
- environment parsing;
- provider wire.

No production scheduling or cancellation framework was added.

## Regression evidence

Preserved:

- active TurnRunner → ASR rejected before stage/spawn/I/O;
- active ASR → normal `turn.input` rejected;
- blocking-provider cancellation;
- normal success at worker-final/canonical-final/turn/model/provider-request `1/1/1/1/1`;
- one TurnRunner;
- single `#submitVoiceFinalTranscript` → `#acceptTurnInput` path;
- MiniMax TTS conformance;
- Python 3.11 floor;
- source-first execution;
- no barge-in, streaming, VAD, retry, fallback, or MiMo TTS.

## CI evidence

Actions `29337325296` passed on the implementation commit:

- Ubuntu verify;
- Windows verify;
- Ubuntu Python 3.11 speech floor;
- Windows Python 3.11 speech floor.

Actions `29338293955` passed on the final evidence head with the same four jobs.

## Carry-ins

1. Neutral artifact identity and label-merge semantics.
2. Attempt versus transport-observed provider connection/request counters.
3. Shutdown termination can still classify some paths as provider failure rather than cancellation.

These are not R0.9-01 close blockers.

## Next action

Proceed with the owner-live procedure in `tasks/R0.9-01-work.md`.

Do not paste the MiMo credential into chat, logs, reports, or commits. Commit only bounded, redacted owner evidence.
