# R0.9-05′ Reused-Dev Lifecycle Re-Review

**Repair commit:** `3337cbed7447e0a8755c3eeb8ef1bffd031d6dfb`  
**Actions run:** `29546014727`  
**Verdict:** **ACCEPTED FOR OWNER RESUME / OPEN**

## Runtime repair

The reused/no-child branch now owns a local `MessageChannel` keep-alive handle.

- no polling;
- no network I/O;
- no subprocess;
- no shell execution;
- no gateway ownership action;
- explicit abort removes the signal listener;
- explicit abort closes both message ports before resolving;
- initialization failure closes both ports before rejecting;
- owned gateway startup/shutdown remains unchanged.

## Regression

Existing Case J now spawns the real source-first CLI vector:

```text
node --import tsx apps/cli/src/bin/fairy.ts dev ... --no-open
```

It proves:

- `Gateway: REUSED` is emitted;
- stop guidance is emitted;
- the CLI remains alive during a bounded observation;
- no gateway start/browser output is emitted;
- stderr contains no unsettled-top-level-await warning;
- the independent gateway remains healthy before and after launcher termination;
- the launcher exits after explicit termination;
- cleanup leaves no launcher child.

## Commit scope

Exact repair commit changes only:

```text
apps/cli/src/dev.ts
apps/cli/test/developer-preview.test.ts
tasks/R0.9-05-work.md
```

`apps/gateway/src/server.ts` remains at blob:

```text
7fe2116db5c9ad95f7ea853c06d7b149ebeab4c2
```

## CI

Run `29546014727` checked out:

```text
3337cbed7447e0a8755c3eeb8ef1bffd031d6dfb
```

All four required jobs passed:

```text
verify (windows-latest)
verify (ubuntu-latest)
Python 3.11 speech floor (windows-latest)
Python 3.11 speech floor (ubuntu-latest)
```

## Owner state

```text
doctor provider-zero: PASS
owned dev: PASS
normal voice: PASS
extra accidental complete voice attempt: RECORDED
reused detection: PASS
reused lifecycle repair: ACCEPTED FOR OWNER PROBE
synthetic denial and later checks: PAUSED UNTIL PROBE
R0.9-05′: OPEN
```

Resume from reused gateway mode only. Do not repeat doctor, owned dev, or voice.

## Carry-in for final cleanup

The current repository history tracks:

```text
tmp/r09-05-owner-mock-20260716-220129.ts
```

This was not added by the repair commit, but it is temporary owner runtime material. Keep it only until the remaining owner checks finish, then delete it before the final owner-evidence commit. It must not remain in the final diff/state.
