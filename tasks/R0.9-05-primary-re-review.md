# R0.9-05′ Primary Re-Review

**Review date:** 2026-07-16  
**Task:** R0.9-05′ — Doctor v0.9, One-Command Dev Start, and Interview Demo Package  
**Initial implementation:** `8164daea47d3b88bc98db33a5fd26a42e9dd92d0`  
**Environment determinism repair:** `83455d88320dfa30567fd768e5288c611943e056`  
**Owner-live documentation repair:** `2b6780afe4f961a65aded66b6e25aa456b79a83e`  
**Windows test-port repair:** `7da05f106633f38df0af406284b6800dd68d162f`  
**Final test-observation repair:** `b0e49611eb6d7398295dd89b40a96917dcf95219`  
**Final Actions run:** `29466381750`  
**Verdict:** **ACCEPTED FOR OWNER-LIVE / OPEN**  
**Owner checks:** **AUTHORIZED**

---

## Decision

The three primary-review blockers are closed:

1. README now documents the two process-scoped real-speech consent flags.
2. The three-minute script and Scenario 1 preserve doctor → consent → dev ordering and cleanup.
3. Case L pins both exact environment-variable names, consent semantics, provider-zero behavior, cleanup, and command ordering.

The two additional Windows CI failures were test-only timing/observation races. Both were repaired without changing runtime code, workflow configuration, test timeouts, Cases A–L count, gateway ownership semantics, cleanup semantics, or `server.ts`.

Owner checks and screenshot capture may begin.

---

## Repair scope

Compared with `83455d8`, cumulative repair changed only:

```text
README.md
apps/cli/test/developer-preview.test.ts
docs/demo/scenarios/01-normal-voice.md
docs/demo/v0.9-demo-script.md
tasks/R0.9-05-primary-review.md
tasks/R0.9-05-work.md
```

No runtime source changed after the accepted `83455d8` state.

---

## Blocker closure

### README

The quick start now requires:

```powershell
pnpm fairy doctor
$env:FAIRY_OWNER_LIVE_ASR = '1'
$env:FAIRY_OWNER_LIVE_TTS = '1'
pnpm fairy dev
```

and cleanup:

```powershell
Remove-Item Env:FAIRY_OWNER_LIVE_ASR -ErrorAction SilentlyContinue
Remove-Item Env:FAIRY_OWNER_LIVE_TTS -ErrorAction SilentlyContinue
```

It states that doctor remains provider-zero, the flags are process-scoped explicit owner consent, setting them alone makes no provider request, and they must not be stored in YAML or committed configuration.

### Demo script and Scenario 1

Both independently carry the two flags, consent wording, provider-zero-until-submission wording, local-only Stop, cleanup, and no ASR benchmark claim.

Before Scenario 2, the demo script stops the live-enabled dev launcher and clears both flags.

### Case L

Case L pins all three guides for:

```text
FAIRY_OWNER_LIVE_ASR
FAIRY_OWNER_LIVE_TTS
process-scoped explicit consent
no request merely from setting flags
valid governed voice submission
no YAML persistence
both cleanup commands
doctor → ASR flag → TTS flag → dev ordering
```

No Case was added, removed, skipped, or weakened.

---

## Interim CI failures

### Run 29463489667

Only Windows Python 3.11 Case I failed.

Root cause:

```text
port:0 selected a Windows dynamic port
→ listener closed
→ port re-entered the dynamic allocation pool
→ another process acquired it
→ real gateway bind returned EADDRINUSE
```

Repair: Cases I/K now use a bounded exclusive range `20000..29999`; Case F retains `port:0`. No timeout or runtime change.

### Run 29465107230

Only Windows full-suite Case K failed.

`runDev()` had already observed real `fairy-running`, but the synthetic browser-failure callback made a second redundant 500 ms health request. Under full-suite load, only that redundant observation timed out.

Repair: Case K records the actual production health result already used by `runDev()`. It retains real gateway start, browser warning, owned shutdown, port release, and source checks.

---

## Final CI

Actions run:

```text
29466381750
```

checked out:

```text
b0e49611eb6d7398295dd89b40a96917dcf95219
```

| Job | Job ID | Result |
|---|---:|---|
| `verify (ubuntu-latest)` | `87520299556` | PASS |
| `verify (windows-latest)` | `87520299560` | PASS |
| `Python 3.11 speech floor (ubuntu-latest)` | `87520299571` | PASS |
| `Python 3.11 speech floor (windows-latest)` | `87520299620` | PASS |

Both Python jobs passed MiniMax, MiMo, Web PTT, and R0.9-05 readiness suites.

---

## server.ts gate

```text
blob: 7fe2116db5c9ad95f7ea853c06d7b149ebeab4c2
non-empty lines: 1,628
pin: <1,629
```

The extraction-first landing gate remains binding.

---

## Owner authorization

Owner checks are authorized against exactly:

```text
b0e49611eb6d7398295dd89b40a96917dcf95219
```

Required evidence:

1. doctor text/JSON with zero provider activity;
2. owned `fairy dev`;
3. one short real browser voice turn;
4. local Stop and reload/replay;
5. Ctrl+C cleanup and port release;
6. reused gateway mode;
7. synthetic secret/local-only zero-everything denial;
8. bounded memory/research/replay;
9. three sanitized PNGs;
10. leakage and residue scans.

Real speech-provider limit:

```text
one successful short browser voice session maximum
```

Do not repeat a successful MiMo/MiniMax call merely to improve screenshots.

---

## Close-time note

`tasks/R0.9-05-work.md` necessarily says final replacement CI was pending when `b0e4961` was created. This is not an owner-check blocker.

Final close must reconcile:

```text
final commit: b0e49611eb6d7398295dd89b40a96917dcf95219
final run: 29466381750
four final job IDs/results
```

---

## Current state

```text
Doctor runtime:             ACCEPTED
Dev runtime:                ACCEPTED
Environment determinism:    ACCEPTED
Owner-live documentation:   ACCEPTED
Windows test-port repair:   ACCEPTED
Health observation repair:  ACCEPTED
Final CI:                   ACCEPTED
Owner checks:               AUTHORIZED
Screenshots:                PENDING
Owner evidence:             PENDING
R0.9-05′:                   OPEN
```

**Primary verdict: ACCEPTED FOR OWNER-LIVE / OPEN.**
