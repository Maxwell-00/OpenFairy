# R0.9-05′ Final Primary Re-Review

**Reviewed HEAD:** `7255cf347ab657239466f372ab4ff12dec4cdccb`  
**Runtime repair commit:** `3337cbed7447e0a8755c3eeb8ef1bffd031d6dfb`  
**Owner-evidence commit:** `d1869d20dd399af66413687f7705dee9ad142dee`  
**Final consistency CI:** Actions `29565851316`  
**Verdict:** **ACCEPTED FOR FABLE COUNTERSIGN / OPEN**

## Scope

The final consistency repair commit changes exactly:

```text
tasks/R0.9-05-work.md
```

The preceding owner-created `bb40568b148e38d20e97b4b3a0e96d5fa4bd7cbb` commit archives prior review/manual documents and is separate from the repair scope.

## Work-report consistency

The work report now accurately records:

- reused lifecycle repair commit and four-job green CI;
- owner-evidence commit and four-job green CI;
- owner checks complete;
- provider-zero doctor;
- owned and reused dev evidence;
- accepted voice session and the disclosed accidental complete attempt;
- S4 ledger at `4/20`, not complete;
- synthetic secret/local-only zero-everything denial;
- MemoryGate/canonical replay Scenario 3;
- recovered local deterministic-model endpoint mismatch with zero public/speech-provider traffic;
- three screenshots and their metadata/leak checks;
- zero committed generated audio/live data;
- zero gateway and speech-worker residue;
- removed temporary mock script;
- R0.9-03+04 and other Tier-2/full-M3 features still deferred;
- Fable countersign still pending;
- no closure claim.

## Owner-procedure deviation

The brief permitted one short real-provider voice session maximum.

Two complete bounded non-sensitive attempts occurred:

1. accepted intended session `ses_01KXNPHJGG9MA4948TZ7Q0C4V4`;
2. accidental complete session `ses_01KXNPDMV2V7NTB6DH6C53889A`, caused by incorrect microphone state.

Only the intended session advanced S4. The deviation is explicit in the work report and must remain visible to the countersigner. No rerun or additional provider call is authorized.

## CI

Actions `29565851316` checked out:

```text
7255cf347ab657239466f372ab4ff12dec4cdccb
```

All required jobs passed:

| Job | Job ID | Result |
|---|---:|---|
| Windows verify | `87838186605` | PASS |
| Ubuntu verify | `87838186613` | PASS |
| Windows Python 3.11 floor | `87838186614` | PASS |
| Ubuntu Python 3.11 floor | `87838186685` | PASS |

## Protected runtime state

`apps/gateway/src/server.ts` remains at blob:

```text
7fe2116db5c9ad95f7ea853c06d7b149ebeab4c2
```

The work report retains the pinned measurements:

```text
SHA-256:        038e7cb70578355e02366e0446513691e86f7529e3897935f6f16b21839931b3
bytes:           64,699
non-empty lines: 1,628
```

No runtime, test, workflow, provider, owner-evidence, screenshot, ledger, or normative document was changed by the final consistency repair.

## Findings

### BLOCKER

None.

### CARRY-IN

- S4 voice-session ledger is `4/20`; it is not complete.
- R0.9-03+04 remains `DEFERRED / NOT DISPATCHABLE`.
- M3-06 faster-whisper/local ASR remains deferred.
- Streaming, VAD, endpointing, barge-in, installer/service/tray, and autonomous workflows remain deferred.

### NOTE

The GitHub connector exposed the committed screenshot files, hashes, sizes, and metadata-scan evidence but did not independently render their pixels in this review. The owner-confirmed pixel scan and committed sanitization evidence are accepted for primary review; the Fable countersigner may inspect the three PNGs directly.

## Final state

```text
Runtime implementation:       ACCEPTED
Deterministic tests:          ACCEPTED
Owner checks:                 ACCEPTED
Owner evidence:               ACCEPTED
Final work report:            ACCEPTED
Final consistency CI:         ACCEPTED
Primary review:               ACCEPTED
Fable countersign:            PENDING
R0.9-05′:                     OPEN
Additional provider calls:    NOT AUTHORIZED
```

R0.9-05′ may be marked CLOSED only after Fable countersigns the disclosed owner-procedure deviation and the complete committed evidence set.

---

## Countersignature — Claude (Fable 5), 2026-07-16

Light-tier countersign per the v0.9 plan (no trust surface in scope), verified inline against git truth `cb17aea..7255cf3` — including the one check the primary review could not perform: **direct pixel inspection of all three committed PNGs** (extracted from the commit, not the working tree).

### Inline verification results

- **`server.ts` byte-identical** across the whole slice (blob `7fe2116d` at both `ea1a628` and `7255cf3`) — the landing-gate clause held exactly as written; ceiling tests untouched.
- **Two unexpected runtime touches examined and accepted as clean refactors**: `packages/voice/src/python.ts` (new) is a pure-data extraction — Python candidate list + version parsing, no spawning, no `child_process`; `index.ts` only re-exports it; the `:658` guard is untouched. `speech-worker-process.ts` (±44) consumes the shared module in place of its private copy — identical error codes and semantics. Net effect: doctor's `runtime.python` and the gateway's discovery now share one source of truth instead of diverging copies. Better than the brief asked.
- **`doctor.test.ts` (±11)** is the brief-mandated migration from text-line assertions to the structured report — the replacement assertions (stable check IDs) are stronger, not weaker.
- **Demo honesty verified in the delivered text**: "Developer Preview" naming throughout; "Stop stops local playback only; it is not barge-in"; "functional path, not an ASR accuracy benchmark"; scenario 2 headlined "**synthetic secret fixture, never owner personal data**" with do-not-commit instructions; the closing segment names the deferrals and the honest ledger.
- **Example config**: `secret://` references only; `example.invalid` placeholder explicitly marked unusable; closed MiMo/MiniMax contracts pinned; `home_regions: [cn]`.
- New suite `developer-preview.launch-v0` lives at `apps/cli/test/developer-preview.test.ts` (818 lines) — CLI-package placement is appropriate for a CLI feature; registered in evals.md by this docs pass with the location noted.

### Screenshot adjudication (the countersigner's direct check)

`02-web-voice-roundtrip.png` is fully clean. `01-doctor-pass.png` and `03-replay-governance.png` are clean in content — credentials shown as class-only, no tokens/keys/headers/request-IDs/mic labels/DevTools; the replay shot shows exactly the governance trace the demo exists to show — **but both include the PowerShell prompt line containing the repository's absolute path (`E:\Claude_Projects\...`)**, a letter-of-the-rule deviation from the 11-item forbidden list. Adjudication: **benign** (no username, no personal data; the rule's intent is identity/secret protection) — **accepted as a recorded deviation, with a recommendation**: a one-minute re-crop of the top line on both PNGs before external use, or an explicit acceptance line in `docs/demo/assets/README.md`. Not a closure blocker.

### Owner-procedure deviation adjudication

The accidental second complete provider session (`ses_01KXNPDMV2…`, wrong microphone state) is **accepted**: bounded, non-sensitive, honestly disclosed in the work report, excluded from the S4 ledger (only the intended session advanced it, now 4/20), and no rerun authorized. This is the disclosure norm working exactly as cultivated — an accident reported plainly beats a clean-looking record.

### Notes

1. The demo script's closing line cites the ledger "3/20 at R0.9-02 close" — accurate as phrased; the owner should speak the then-current number in a live interview.
2. CI `29565851316` four jobs green at the reviewed HEAD; the recovered local endpoint-mismatch during owner checks generated zero public/provider traffic — consistent with §4.3.

### Docs pass — applied with this countersignature

`evals.md`: `developer-preview.launch-v0` registry row + registration status (Cases A–L; location `apps/cli/test`; keyless; doctor/dev shared registry; ownership-mode and shutdown-cleanup coverage). No other spec requires changes — the demo package under `docs/demo/` is itself this slice's documentation.

### Verdict: R0.9-05′ ACCEPTED WITH NOTES / CLOSED

The sprint now has its deliverable for the actual audience: a doctor that proves the machine ready without touching a provider, a launcher that owns or defers honestly, and a three-minute script whose crown jewel is a governed refusal. **Next: gate the R0.9-06′ brief** (light release gate: deferral ledger — incl. R0.9-03+04, M3-06, the plan-file consolidation from R0.9-02 gate D1, and the screenshot-deviation record; version naming; ROADMAP/PRD/ARCHITECTURE overlay notes land there per the plan gate).
