# R0.9-05′ Primary Implementation Review

**Review date:** 2026-07-15  
**Gated brief:** `tasks/R0.9-05-prime-doctor-dev-start-demo-package.md`  
**Dispatch baseline:** `cb17aeaf38050aa5e18d5be737e571298e1d634e`  
**Initial implementation:** `8164daea47d3b88bc98db33a5fd26a42e9dd92d0`  
**Determinism repair:** `83455d88320dfa30567fd768e5288c611943e056`  
**Failed Actions:** `29421324983`  
**Replacement Actions:** `29427255034`  
**Verdict:** **CHANGES REQUIRED — DEMO ACTIVATION DOCUMENTATION + REGRESSION PIN**  
**Runtime implementation:** **ACCEPTED**  
**Owner checks:** **NOT YET AUTHORIZED**  
**Task status:** **OPEN**

---

## 1. Decision

The doctor/dev runtime implementation and CI determinism repair are accepted.

No change is required to:

- `apps/cli/src/doctor.ts`;
- `apps/cli/src/dev.ts`;
- Python discovery/floor extraction;
- gateway ownership Modes A/B/C;
- source-first spawning;
- Windows cleanup;
- provider-zero readiness behavior;
- `apps/gateway/src/server.ts`.

However, the primary product path is not yet executable from the committed README/demo instructions in a clean shell.

The normal voice demo requires the existing explicit production gates:

```text
FAIRY_OWNER_LIVE_ASR=1
FAIRY_OWNER_LIVE_TTS=1
```

The committed README, three-minute script, and Scenario 1 do not tell the operator to set those gates before `pnpm fairy dev`.

Without them, the gateway intentionally fails closed after clearance/egress and before credential/worker/provider execution:

```text
ASR: asr_owner_live_required
TTS: tts_owner_live_required
```

Therefore the advertised normal bilingual voice scenario cannot succeed by following the committed quick start alone.

This is a release/demo blocker, not a provider or architecture defect.

A second required deliverable remains stale: `tasks/R0.9-05-work.md` still says replacement CI is pending even though run `29427255034` is green.

Owner screenshots, live voice, and final demo capture must wait for the bounded repair below.

---

## 2. Git-backed scope

The implementation commit added or changed the expected Tier-1 surfaces:

```text
.github/workflows/ci.yml
README.md
apps/cli/package.json
apps/cli/src/bin/fairy.ts
apps/cli/src/dev.ts
apps/cli/src/doctor.ts
apps/cli/src/index.ts
apps/cli/test/developer-preview.test.ts
apps/cli/test/doctor.test.ts
apps/gateway/src/speech-worker-process.ts
docs/demo/*
examples/fairy.v0.9.yaml
packages/voice/src/index.ts
packages/voice/src/python.ts
pnpm-lock.yaml
scripts/start-gateway.mjs
tasks/R0.9-05-work.md
```

No forbidden implementation area changed.

The CI repair commit changed only:

```text
apps/cli/test/developer-preview.test.ts
tasks/R0.9-05-work.md
```

It removed ambient environment inheritance from the focused suite without changing runtime code, timeouts, workflow steps, or Cases A–L.

---

## 3. Accepted implementation findings

### 3.1 `server.ts` gate — PASS

At `83455d8`:

```text
blob:      7fe2116db5c9ad95f7ea853c06d7b149ebeab4c2
SHA-256:   038e7cb70578355e02366e0446513691e86f7529e3897935f6f16b21839931b3
non-empty: 1,628
pin:       <1,629
```

`apps/gateway/src/server.ts` is byte-identical to the accepted R0.9-02 state.

The extraction-first landing gate remains binding for every future slice.

### 3.2 Shared readiness registry — PASS

Doctor and dev both consume `evaluateReadiness()`.

The fixed registry contains the required 16 ordered checks:

```text
runtime.node
runtime.pnpm
runtime.python
config.load
config.main-role
config.gateway-auth
config.speech-asr
config.speech-tts
storage.data-dir
storage.artifacts
storage.sessions
network.gateway-port
runtime.gateway-health
web.assets
optional.container-runtime
optional.research
```

Status and exit laws are bounded:

```text
pass / warn / fail
warnings -> exit 0
any fail -> exit 1
JSON -> one report object from the CLI renderer
```

### 3.3 Readiness provider-zero contract — PASS

Doctor/dev readiness code contains one network target only:

```text
http://127.0.0.1:<validated-port>/health
```

No model, MiMo, MiniMax, or research provider request implementation exists in doctor/dev.

The focused suite proves zero:

```text
model connections
MiMo connections
MiniMax connections
research connections
sessions
artifacts
turns
```

### 3.4 Secret hygiene — PASS

The implementation rejects unsafe readiness flags, including:

```text
--token
--api-key
--secret
--live
--probe-provider
--base-url
--host
```

Reports use credential presence/class only.

Gateway token and provider credentials are absent from:

- doctor text/JSON;
- dev console;
- gateway argv;
- browser URL;
- example config values;
- committed demo evidence.

### 3.5 Shared Python law — PASS

`packages/voice/src/python.ts` owns:

```text
Linux/macOS: python3 -> python
Windows:     python3 -> python -> py -3
test/CI:     FAIRY_TEST_PYTHON candidate only
floor:       Python >=3.11
```

The speech supervisor and doctor use the same candidate/version law.

The existing test/CI-only override gate remains intact.

### 3.6 Gateway ownership — PASS

The implementation distinguishes:

| Mode | Result |
|---|---|
| A — port available | start and own the existing source-first launcher |
| B — healthy Fairy gateway | reuse; never signal it |
| C — occupied/unhealthy/non-Fairy | fail before gateway/browser spawn |

The launch chain remains:

```text
pnpm fairy dev
-> scripts/run-cli.mjs
-> tsx CLI source
-> apps/cli/src/dev.ts
-> scripts/start-gateway.mjs
-> tsx gateway source
```

No runtime `dist`, second launcher, second listener, host override, or credential argv was added.

### 3.7 Shutdown — PASS pending owner observation

Deterministic tests prove:

- owned child graceful IPC shutdown;
- Windows tree-kill fallback;
- bounded exit waits;
- port release;
- reused gateway remains alive;
- browser-open failure is WARN.

Actual Windows browser opening and interactive Ctrl+C remain proper owner checks.

### 3.8 Example config / README / demo content — PASS except activation blocker

Accepted:

- schema-valid example config;
- only `secret://` values;
- closed MiMo/MiniMax profiles;
- Developer Preview naming;
- S4 ledger `3/20`;
- synthetic secret denial;
- local-only Stop;
- no ASR benchmark claim;
- three-minute structure;
- three demo scenarios;
- screenshot sanitization rules.

Blocked item:

- normal live voice activation flags are omitted.

---

## 4. CI evidence

### 4.1 Initial failure — correctly diagnosed

Run `29421324983` failed only Case D across all four jobs.

Root cause:

```text
test harness inherited complete process.env
+ GitHub CI=true
+ FAIRY_TEST_PYTHON in floor lanes
-> production-isolation simulation still qualified as CI
-> test override correctly remained enabled
```

The repair replaced ambient inheritance with a narrow platform allowlist and directly tests poison-variable rejection.

### 4.2 Replacement CI — PASS

Run:

```text
29427255034
```

checked out:

```text
83455d88320dfa30567fd768e5288c611943e056
```

| Job | Job ID | Result |
|---|---:|---|
| `verify (windows-latest)` | `87393768991` | PASS |
| `verify (ubuntu-latest)` | `87393810819` | PASS |
| `Python 3.11 speech floor (windows-latest)` | `87393770723` | PASS |
| `Python 3.11 speech floor (ubuntu-latest)` | `87393796265` | PASS |

Both Python lanes passed:

```text
MiniMax TTS provider conformance
MiMo ASR provider conformance
R0.9-02 Web PTT end-to-end
R0.9-05 doctor/dev readiness
```

---

## 5. BLOCKERS

### BLOCKER 1 — normal voice demo omits explicit owner-live activation

Affected files:

```text
README.md
docs/demo/v0.9-demo-script.md
docs/demo/scenarios/01-normal-voice.md
```

The gateway intentionally requires:

```powershell
$env:FAIRY_OWNER_LIVE_ASR = '1'
$env:FAIRY_OWNER_LIVE_TTS = '1'
```

before launching the gateway that will perform a real browser voice turn.

The committed instructions currently proceed directly from credentials/doctor to:

```powershell
pnpm fairy dev
```

This starts a healthy gateway, but a real voice submission cannot reach MiMo or MiniMax.

#### Required repair

Document an explicit sequence:

```powershell
pnpm fairy doctor

# Explicit, process-scoped consent for real speech-provider execution.
$env:FAIRY_OWNER_LIVE_ASR = '1'
$env:FAIRY_OWNER_LIVE_TTS = '1'

pnpm fairy dev
```

Also document cleanup:

```powershell
Remove-Item Env:FAIRY_OWNER_LIVE_ASR -ErrorAction SilentlyContinue
Remove-Item Env:FAIRY_OWNER_LIVE_TTS -ErrorAction SilentlyContinue
```

Required wording:

- flags are process-scoped explicit consent;
- doctor remains provider-zero;
- setting flags alone makes no provider call;
- a provider call occurs only after a valid governed voice request;
- do not commit or persist them in YAML;
- Scenario 2 uses a separate clean shell or clears the flags before its synthetic denial procedure.

### BLOCKER 2 — no regression pin for the activation instructions

Case L currently pins Developer Preview naming, S4 honesty, synthetic secret wording, local Stop, and ASR benchmark honesty, but not the two required owner-live gates.

Add discriminating assertions that the README, demo script, and Scenario 1 contain both exact environment-variable names and explain process-scoped explicit activation.

Do not weaken any existing Case L assertion.

### BLOCKER 3 — work report still says replacement CI is pending

Update:

```text
tasks/R0.9-05-work.md
```

Record:

```text
repair commit: 83455d88320dfa30567fd768e5288c611943e056
replacement run: 29427255034
four job names / IDs / PASS
```

Change only the now-stale CI status. Owner screenshots and owner-live remain pending.

---

## 6. Required repair scope

Expected files only:

```text
README.md
docs/demo/v0.9-demo-script.md
docs/demo/scenarios/01-normal-voice.md
apps/cli/test/developer-preview.test.ts
tasks/R0.9-05-work.md
```

No runtime change is required.

Do not modify:

```text
apps/gateway/src/server.ts
apps/cli/src/doctor.ts
apps/cli/src/dev.ts
scripts/start-gateway.mjs
packages/voice/*
workers/speech/*
.github/workflows/ci.yml
docs/specs/*
docs/v0.9/*
tasks/R0.9-03+04-DEFERRED-bounded-morning-briefing.md
docs-zh/
```

---

## 7. Required validation

Run:

```powershell
pnpm lint
pnpm -r typecheck
pnpm --filter @fairy/cli test:developer-preview
pnpm --filter @fairy/cli test -- --reporter=verbose
pnpm -r test
pnpm dep-check
git diff --check
```

No real provider call is required.

After owner commit/push, all four CI jobs must pass again.

---

## 8. Owner-check authorization

```text
NOT AUTHORIZED
```

Do not yet:

- set owner-live flags for a demo run;
- call MiMo or MiniMax;
- capture the three screenshots;
- append an S4 ledger row;
- commit owner evidence.

After the bounded repair commit and green replacement CI, primary re-review should be limited to the five-file repair. If green and accurate, owner checks can begin without another full implementation review.

---

## 9. Current state

```text
Doctor/runtime implementation: ACCEPTED
Dev/runtime implementation:    ACCEPTED
CI determinism repair:          ACCEPTED
Replacement CI:                 ACCEPTED
Demo activation instructions:   BLOCKED
Work report CI reconciliation:  BLOCKED
Owner checks:                   NOT AUTHORIZED
R0.9-05′:                       OPEN
```

**Primary verdict: CHANGES REQUIRED.**
