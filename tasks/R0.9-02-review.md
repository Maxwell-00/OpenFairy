# R0.9-02 Final Primary Close

**Review date:** 2026-07-15  
**Task:** R0.9-02 — localhost Web push-to-talk + MiMo ASR + MiniMax MP3 playback  
**Accepted implementation:** `bc88889d9e30f2b16b5d304382cf1ae02a801a4f`  
**Owner evidence archive:** `9d88c74830600fb510922154f380ca1f9b3f8a92`  
**Final reconciliation commit:** `e36d48488d72c3c3ba0b4172c0491b093a55d5df`  
**Final Actions run:** `29410020242`  
**Verdict:** **ACCEPTED WITH NOTES / CLOSED**

---

## 1. Final decision

R0.9-02 is closed.

The implementation, blocker repair, deterministic CI, real browser owner-live evidence, authentication and ownership checks, leakage and residue scans, bounded voice-session ledger, final work-report reconciliation, and final four-job CI all satisfy the task acceptance contract.

No additional implementation change, browser session, MiMo request, MiniMax request, or owner-live rerun is required.

The former documentation-only blocker is closed.

---

## 2. Final reconciliation scope

Commit:

```text
e36d48488d72c3c3ba0b4172c0491b093a55d5df
```

changed only:

```text
tasks/R0.9-02-final-primary-review.md
tasks/R0.9-02-work.md
```

The required work report now records:

- repair commit `bc88889...`;
- replacement CI `29398291297`;
- replacement job names, IDs, and PASS results;
- owner evidence archive `9d88c74...`;
- evidence CI `29408324951`;
- evidence job names, IDs, and PASS results;
- Chrome manual session;
- Chrome exact 60-second automatic-stop session;
- Edge manual session;
- authentication and ownership PASS;
- leakage and residue PASS;
- ledger accumulation `3/20`;
- bounded ASR-quality note;
- final close pending only this reconciliation commit and CI.

No implementation source, provider contract, worker, ArtifactRegistry, protocol, plan, workflow, owner evidence JSON, ledger, or `docs-zh/` file changed.

---

## 3. Final CI evidence

Actions run:

```text
29410020242
```

checked out:

```text
e36d48488d72c3c3ba0b4172c0491b093a55d5df
```

Required jobs:

| Job | Job ID | Result |
|---|---:|---|
| `verify (ubuntu-latest)` | `87335500210` | PASS |
| `verify (windows-latest)` | `87335480301` | PASS |
| `Python 3.11 speech floor (ubuntu-latest)` | `87335481176` | PASS |
| `Python 3.11 speech floor (windows-latest)` | `87335509903` | PASS |

Normal verify passed:

```text
install
lint
recursive typecheck
recursive test
dependency rules
```

Both Python 3.11 speech-floor jobs passed:

```text
M3-05 MiniMax TTS provider conformance
R0.9-01 MiMo ASR provider conformance
R0.9-02 Web PTT end-to-end
```

The Windows normal lane and Windows Python 3.11 lane independently logged the exact final commit SHA.

---

## 4. Accepted owner-live evidence

### Chrome manual

```text
browser: Chrome 150.0.7871.124
duration: 6,970 ms
WAV: 223,104 bytes
speech.asr.final: 1
turn.input: 1
model request: 1
turn.final: 1
speech.tts.chunk: 1
reload/replay: PASS
stored MP3 replay: PASS
local Stop: PASS
```

### Chrome automatic stop

```text
warning: 50 seconds
countdown: 55 seconds
automatic stop: 60 seconds
duration: 60,000 ms
samples: 960,000
WAV: 1,920,044 bytes
later pointer release produced second turn: no
recorder/media residue: none
speech.asr.final: 1
turn.input: 1
model request: 1
turn.final: 1
speech.tts.chunk: 1
```

### Edge manual

```text
browser: Edge 150.0.4078.65
duration: 7,592 ms
WAV: 242,988 bytes
speech.asr.final: 1
turn.input: 1
model request: 1
turn.final: 1
speech.tts.chunk: 1
reload/replay: PASS
stored MP3 replay: PASS
local Stop: PASS
```

---

## 5. Authentication and ownership

Accepted results:

```text
wrong-token session list: 401
wrong-token upload: 401
wrong-token WebSocket: close 4401
wrong-token MP3 fetch: 401
valid token + wrong session: 404
arbitrary artifact ID: 404
traversal-shaped request: 404
denied artifact bytes: 0
model request delta: 0
provider request delta: 0
```

Session-owned MP3 access is proven by canonical `speech.tts.chunk.payload.audio_ref`, not by changing neutral content-addressed artifact identity.

---

## 6. Leakage and cleanup

Accepted findings:

```text
credential leak: false
gateway-token leak: false
secret-ref leak: false
Authorization/api-key header leak: false
data URL leak: false
long base64 leak: false
raw audio marker leak: false
provider envelope leak: false
temporary path leak: false
artifact path leak: false
object URL leak: false
new MiMo temp roots: 0
new MiniMax temp roots: 0
worker process residue: 0
generated audio committed: 0
microphone device label recorded: false
```

---

## 7. Acceptance matrix

| Area | Result |
|---|---|
| One TurnRunner and existing turn path | PASS |
| Source-first native browser ESM | PASS |
| Same gateway listener and exact static allowlist | PASS |
| No React/Vite/framework/runtime dependency | PASS |
| Token memory-only browser handling | PASS |
| HTTP and WebSocket authentication | PASS |
| Canonical 16 kHz mono PCM16 WAV | PASS |
| 50-second warning / 55-second countdown | PASS |
| Exact 60-second hard stop | PASS |
| Bounded upload and strict gateway WAV parser | PASS |
| Existing MiMo ASR path | PASS |
| Existing TurnRunner model path | PASS |
| Shared visible-final MiniMax TTS path | PASS |
| Authenticated session-owned MP3 fetch | PASS |
| Local-only playback Stop | PASS |
| Canonical reload/replay | PASS |
| Provider/private field suppression | PASS |
| No raw audio/base64/token/provider body in JSONL | PASS |
| Deterministic Web suite Cases A–N | PASS |
| Ubuntu and Windows verify | PASS |
| Ubuntu and Windows Python 3.11 speech floor | PASS |
| Chrome manual owner session | PASS WITH NOTE |
| Chrome exact auto-stop owner session | PASS |
| Edge manual owner session | PASS WITH NOTE |
| Authentication/ownership owner checks | PASS |
| Leakage/residue owner checks | PASS |
| Three bounded ledger rows | PASS |
| Final work-report reconciliation | PASS |
| Final reconciliation CI | PASS |

---

## 8. Notes and carry-ins

### R0.9-02-C1 — artifact content identity

Content-addressed identity and general cross-label merge semantics remain the R0.9-01 carry-in.

R0.9-02 correctly proves playback ownership through the canonical session reference.

### R0.9-02-C2 — provider observability

Owner evidence proves successful real execution and the no-retry route contract.

A later observability slice may distinguish provider attempts from transport-observed requests more explicitly.

### R0.9-02-C3 — ASR quality

The two short bilingual sessions produced non-empty but imperfect transcripts. The owner also noted that articulation may have been unclear.

This is not a functional release blocker. R0.9-02 makes no WER or ASR-quality benchmark claim.

### R0.9-02-C4 — future S4 accumulation

Current bounded real-session ledger:

```text
3 / 20
```

The future 20-session S4 threshold remains incomplete and is not waived.

### R0.9-02-C5 — server pressure

`server.ts` remains at the existing non-empty line ceiling boundary.

Future gateway responsibilities should be extracted rather than compressed into the same file.

---

## 9. Closed state

```text
Implementation:            ACCEPTED
Primary repair:            ACCEPTED
Replacement CI:            ACCEPTED
Owner-live:                ACCEPTED
Evidence hygiene:          ACCEPTED
Evidence CI:               ACCEPTED
Work report:               ACCEPTED
Final reconciliation CI:   ACCEPTED
R0.9-02:                   CLOSED
```

**Final primary verdict: ACCEPTED WITH NOTES / CLOSED.**

Next governance steps:

1. Fable/Opus countersign;
2. reviewer-owned English documentation pass;
3. proceed to R0.9-05′.

---

## Countersignature — Claude (Fable 5), 2026-07-15

Code-level cross-check delegated to an opus subagent (9-item checklist over the cumulative diff `ba3cc3a..e36d484` — 37 files, +7406/−34 — with the blocker-repair commit examined line-by-line); the tightest standing risk (`server.ts` ceiling) additionally verified directly by this reviewer. **9/9 PASS, zero FAILED/vacuous items; all 8 reviewer-gate clauses confirmed in code.** Session note for the record: the Cowork mount served stale pre-R0.9-02 snapshots of several runtime files this session (deletions-only working-tree diffs vs HEAD — the handbook §3.1 signature at whole-file scale); all verification ran exclusively on `git show` truth, as always.

### Highlights

- **Every trust surface at the browser boundary is behaviorally proven**: the static handler has no fs-joins to traverse (exact `Map` allowlist; `%2e%2e%2f` → 404); the token has nowhere to persist (storage APIs absent AND test-asserted, module-local variable wiped on disconnect/pagehide, zero logging statements gateway-side); a `4401` rejection leaves zero sessions/artifacts/ops; upload gates all precede buffering with the streaming reader aborting at byte 1,920,044; each WAV malformation class has its own rejection fixture; and MP3 ownership is proven by the session's own canonical `speech.tts.chunk.audio_ref` reference — respecting R0.9-01-C1 by never touching content-addressed identity — with a full 404 matrix (cross-session, tampered byte, symlink, registry-path-escape) and no metadata/labels/paths in any error body.
- **The blocker round strengthened the slice again**: `bc88889` replaced mutable session selection with generation-bound identity (a stale ASR ACK or fetch can no longer post into a newer session), converted the dead-end error state into bounded failed/retry, and swapped a flaky `setTimeout(20)` for a code-gated deterministic barrier — web unit tests went 7→13, and nothing was loosened. Three-for-three now: every primary-review blocker round in this sprint has tightened the codebase.
- **Honesty preserved where it matters for the demo**: stop is local-only (test asserts no cancel/interrupt/socket activity in the stop path); the exact-60s auto-stop is sample-clock-driven (recorder source contains no `setTimeout`/`setInterval`); the S4 ledger records 3/20 and the owner summary explicitly refuses to claim the threshold.
- Projection discipline: `surface=web-v0` sockets get exactly three ops and a whitelist-projected, poison-redacted view; reloads reconstruct everything from `session.attach` replay — the browser holds no authoritative state. Failure mapping collapses raw internal statuses to a generic `request_failed` at the projection edge while keeping the visible final text.

### Named carry-in with landing gate — `server.ts` ceiling headroom is SPENT

Verified directly: 1,628 non-empty lines against the double-pinned `< 1,629` ceiling (`voice.mimo-asr-provider.test.ts:331` + `voice.web-ptt.test.ts:369`, value unchanged — no self-raising). One line of headroom, held by single-line combined declarations (a documented smell). **Landing gate: any future slice that touches `apps/gateway/src/server.ts` must extract a responsibility into an owned module BEFORE adding — compression is no longer an option, and the brief gate for that slice must carry this clause.** R0.9-05′/06′ should not need to touch the file at all.

### Notes

1. The subagent flagged the suite's source-string assertions (readFile greps) as weaker guards than the behavioral tests they supplement — true, and acceptable: every load-bearing property also has a behavioral assertion; the greps are drift tripwires, not the proof.
2. The gate's D3 addendum (WS upgrade query never logged) is satisfied trivially and structurally: the gateway contains zero logging statements.
3. Constants are single-sourced per trust boundary (browser `wav.js` / gateway `web-voice-wav.ts`) — intentional, since the gate itself demanded an independent gateway parser that does not trust browser code.
4. Primary carry-ins C1–C5 endorsed as recorded; C5 is superseded by the sharper landing-gate clause above.

### Docs pass — applied with this countersignature

`voice-pipeline.md` (R0.9-02 status: Tier-1 demo loop closed; browser recording contract; projection + ownership rules; the ceiling pressure note), `data-governance.md` (the web client is a projection, not a peer — token/ownership/redaction rules), `evals.md` (`voice.web-ptt-v0` registry row + registration status). ROADMAP/PRD/ARCHITECTURE notes remain deferred to the v0.9 release gate.

### Verdict: R0.9-02 ACCEPTED / CLOSED

**The Tier-1 demo loop is closed.** Hold the button in a browser, speak in two languages, and everything the trust stack promised since M2 happens where an interviewer can see it: the token can't stick, the audio can't lie about its format, a denied recording never touches disk, the artifact can't be fetched across sessions, and `fairy replay` shows every governed step. **Next: gate the R0.9-05′ brief** (doctor + one-command dev start + demo package — no installer; it should not touch `server.ts`; the demo script's three scenarios are the deliverable that matters most for the owner's deadline).
