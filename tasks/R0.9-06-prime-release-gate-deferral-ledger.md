# R0.9-06′ — OpenFairy v0.9 Developer Preview Release Gate and Deferral Ledger

**Status:** GATED 2026-07-16 by the reviewer (Claude Fable 5) — D1–D4 all accepted as recommended + one scoped docs-ownership exception + one ledger-ID addition; gate record in `tasks/R0.9-06-prime-brief-review.md`. DISPATCHABLE.  
**Track:** OpenFairy v0.9 Developer Preview / Portfolio Release Track — Tier 1 release gate  
**Baseline commit:** `e39a4d672b29bec55d01b527abc59dca9b062363`  
**Predecessors:** R0.9-01, R0.9-02, and R0.9-05′ are `ACCEPTED WITH NOTES / CLOSED`  
**External name:** `OpenFairy v0.9 Developer Preview`  
**Internal name:** `Portfolio Release Track`  
**Review depth:** lightweight primary review + lightweight Fable countersign  
**Runtime/provider scope:** none  
**Owner-live provider calls:** forbidden  
**Target:** repository/documentation release consistency on Ubuntu and Windows CI

---

## 1. Objective

Close the Tier-1 release overlay without adding another product capability.

R0.9-06′ must turn the already-closed R0.9-01 / R0.9-02 / R0.9-05′ deliverables into one coherent, interview-ready repository state by:

1. establishing one authoritative v0.9 plan file;
2. publishing a complete, expiring deferral ledger;
3. reconciling release naming and the real S4 count;
4. adding bounded current-release overlays to ROADMAP, PRD, and ARCHITECTURE;
5. recording accepted screenshot and owner-procedure deviations;
6. proving every release claim resolves to committed evidence;
7. preserving all runtime, trust, provider, protocol, and milestone invariants.

This is a release-governance slice. It must not build another voice feature, Tier-2 Morning Briefing, subagents, local ASR, streaming, barge-in, installer/packaging, or a GitHub Release.

The intended repository story is:

```text
OpenFairy v0.9 Developer Preview
→ Tier-1 feature slices closed
→ governed browser voice loop demonstrable
→ doctor/dev/replay evidence committed
→ current limitations explicit
→ every deferral has a landing gate
→ no false claim of complete M3/M4/M5 or v1.0
```

---

## 2. Read first

Codex must read the committed versions at the exact baseline:

```text
CLAUDE.md
REVIEWER-HANDBOOK.md
README.md
package.json

docs/DECISIONS.md
docs/ROADMAP.md
docs/PRD.md
docs/ARCHITECTURE.md
docs/specs/evals.md

docs/v0.9/v0.9-plan-review.md
docs/v0.9/OpenFairy-v0.9-final-tiered-plan.md
docs/v0.9/OpenFairy-v0.9-final-tiered-plan-60s-amended.md

docs/demo/v0.9-demo-script.md
docs/demo/interview-project-summary.md
docs/demo/assets/README.md

tasks/M2/M2-exit-review.md

tasks/M3/M3-06-faster-whisper-local-asr.md
tasks/M3/M3-06-brief-review.md

tasks/R0.9-01-review.md
tasks/R0.9-02-brief-review.md
tasks/R0.9-02-review.md
tasks/R0.9-03+04-DEFERRED-bounded-morning-briefing.md
tasks/R0.9-05-work.md
tasks/R0.9-05-final-primary-re-review.md

tasks/owner-checks/R0.9-02/voice-session-ledger.jsonl
tasks/owner-checks/R0.9-05/screenshots.json
tasks/owner-checks/R0.9-05/owner-live-summary.json

apps/cli/test/developer-preview.test.ts
```

Use committed Git truth:

```text
git show
git diff
git ls-tree
git rev-parse
git status
```

A mounted or cached workspace view is low-trust when it conflicts with Git.

Do not read, edit, recreate, translate, or otherwise touch `docs-zh/`.

---

## 3. Current state to preserve

### 3.1 Closed Tier-1 implementation slices

```text
R0.9-01  Xiaomi MiMo non-streaming artifact-backed ASR       CLOSED
R0.9-02  authenticated localhost Web push-to-talk            CLOSED
R0.9-05′ doctor + source-first dev launcher + demo package   CLOSED
```

R0.9-05′ close commit:

```text
e39a4d672b29bec55d01b527abc59dca9b062363
```

The R0.9-05′ countersign accepted:

- the bounded accidental second provider attempt as a disclosed owner-procedure deviation;
- the three committed screenshots;
- the repository absolute-path prompt line in screenshots 1 and 3 as a benign recorded deviation;
- the current S4 ledger at `4/20`;
- no additional provider rerun.

Do not reopen any closed slice.

### 3.2 Current real voice-session count

The authoritative bounded ledger is:

```text
tasks/owner-checks/R0.9-02/voice-session-ledger.jsonl
```

At baseline:

```text
accepted rows: 4
required:      20
status:        incomplete
```

Historical facts must remain distinguishable:

```text
R0.9-02 close count: 3/20
current release count: 4/20
S4 landing gate: incomplete
```

Fixture sessions do not count. The accidental second complete R0.9-05′ attempt is not a second accepted S4 row.

### 3.3 Plan-file authority debt

R0.9-02 gate decision D1 established:

```text
docs/v0.9/OpenFairy-v0.9-final-tiered-plan-60s-amended.md
```

as the binding R0.9-02 overlay while requiring R0.9-06′ to consolidate the repository back to one plan file.

### 3.4 Top-level documents are aspirational

`docs/PRD.md`, `docs/ROADMAP.md`, and `docs/ARCHITECTURE.md` describe the full intended v1 system. They contain target-state concepts not shipped in v0.9, including streaming ASR/TTS, Opus streaming, VAD/endpointing, Lane A/B, ack bank, barge-in, tray/IM clients, scheduler/workflows, subagents, installer, and v1.0 hardening.

R0.9-06′ must add a bounded current-release overlay. It must not rewrite the future design as cancelled.

### 3.5 Protected runtime state

`apps/gateway/src/server.ts` is pinned:

```text
blob:             7fe2116db5c9ad95f7ea853c06d7b149ebeab4c2
SHA-256:          038e7cb70578355e02366e0446513691e86f7529e3897935f6f16b21839931b3
bytes:            64,699
non-empty lines:  1,628
```

R0.9-06′ must not modify it. No production runtime source should change.

---

## 4. Gate decisions required before dispatch

Fable must record explicit decisions on the following items. The recommendations below are the drafter's proposed binding answers.

### D1 — Single authoritative v0.9 plan

**Recommendation: consolidate and delete the temporary amendment file.**

Required mechanism:

1. merge the complete 60-second R0.9-02 contract into `docs/v0.9/OpenFairy-v0.9-final-tiered-plan.md`;
2. preserve a provenance note stating that the original plan used 90 seconds, R0.9-02 gate D1 made the 60-second amendment binding, and R0.9-06′ consolidated it;
3. after content parity is verified, delete `docs/v0.9/OpenFairy-v0.9-final-tiered-plan-60s-amended.md`;
4. do not edit closed R0.9-02 review records that refer to the historical file.

This brief authorizes deletion only after Fable explicitly accepts D1.

### D2 — Screenshot path deviation

**Recommendation: preserve canonical screenshots and record the accepted deviation.**

Do not edit or recrop the PNGs in this slice. Add an explicit note to `docs/demo/assets/README.md`:

- screenshot 2 is clean;
- screenshots 1 and 3 contain a PowerShell prompt with a repository absolute path;
- no username, credential, personal data, token, request ID, or device label is present;
- R0.9-05′ accepted this as benign;
- public derivative material should crop that line;
- canonical owner-evidence PNGs remain unchanged.

### D3 — Version and tag semantics

**Recommendation: documentation identity only.**

Binding names:

```text
external: OpenFairy v0.9 Developer Preview
internal: Portfolio Release Track
```

Do not change workspace package versions, publish packages, create a Git tag/GitHub Release, claim production stability, or claim v1.0. After closure, the owner may separately create an annotated tag such as `v0.9.0-dev-preview`; tag creation is outside this task.

### D4 — Tier-2 activation

**Recommendation: not activated.**

R0.9-03+04 remains `DEFERRED / NOT DISPATCHABLE`; S4 is `4/20`, a fresh brief/full gate is required, and the owner has not authorized workflow implementation. M3-06 remains gated but deferred to full M3/v1.0.

---

## 5. Deliverable A — Canonical plan consolidation

Update:

```text
docs/v0.9/OpenFairy-v0.9-final-tiered-plan.md
```

### 5.1 R0.9-02 contract

The canonical plan must contain:

```text
getUserMedia + AudioContext PCM capture
→ resample to 16 kHz mono PCM16
→ encode complete WAV in browser
→ stop / auto-stop at 60 seconds
→ authenticated complete-file upload
→ input artifact
→ MiMo ASR
→ one TurnRunner
→ MiniMax TTS
→ authenticated MP3 playback
```

And:

```text
recommended duration: <=30 seconds
warning:              50 seconds
visible countdown:    55 seconds
hard stop:            60 seconds
max samples:          960,000
max WAV bytes:        1,920,044
```

The 60-second bound must be sample-count authoritative, repository-owned, non-configurable through YAML/CLI/query/browser override, explicitly supersede 90 seconds, and fail closed before artifact registration/provider I/O for over-limit uploads.

### 5.2 Current Tier-1 state

Add a concise status note:

```text
R0.9-01  CLOSED
R0.9-02  CLOSED
R0.9-05′ CLOSED
R0.9-06′ release gate in progress until review/countersign
```

Do not self-close R0.9-06′.

### 5.3 Release identity

Preserve the external/internal names and explicitly state that the overlay does not complete M3, M4, M5, or v1.0.

### 5.4 Single-authority proof

After merge:

- verify every 60-second amendment requirement is present;
- verify no live R0.9-02 requirement depends on the deleted file;
- delete the amendment file only under accepted D1;
- record the comparison method in the work report;
- do not rewrite historical review files.

---

## 6. Deliverable B — `docs/v0.9-deferrals.md`

Create:

```text
docs/v0.9-deferrals.md
```

This is the release's authoritative deferral and accepted-deviation ledger.

### 6.1 Header contract

State:

- external/internal names;
- baseline and last-updated commit placeholder;
- Tier-1 scope;
- the ledger does not authorize implementation;
- every deferral has an expiry/landing gate;
- carrying debt beyond its landing gate requires a new explicit decision;
- silence is never a waiver;
- closed reviews remain historical source of truth.

### 6.2 Required fields

Each active deferral row must include:

| Field | Meaning |
|---|---|
| ID | stable ledger ID |
| Source | originating review/plan |
| Status | deferred / gated-but-deferred / blocked-by-landing-gate |
| Reason | why it was not built now |
| User impact | capability absent today |
| Security/trust impact | whether deferral reduces or preserves risk |
| Current evidence | what exists today |
| Landing gate | named future task/gate |
| Expiry condition | condition forcing reconsideration |
| Waiver rule | explicit recorded decision required |

Do not use vague `later`, `future`, `TBD`, or `when needed` without a named gate and condition.

### 6.3 Mandatory M2 carry-ins

Include at least:

```text
M2-S4-20
M2-PERSONA-JUDGE-90
M2-VECTOR-200K
M2-MEMORY-CANARY-CONTRADICTION
M2-GOVERNANCE-FRICTION-SOAK
M2-REAL-VISION-OCR
M2-LEARNED-SKILL-ACTIVATION
```

Binding conditions:

- S4: current `4/20`; `>=20` real sessions before M4 entry unless an explicit gate decision says otherwise;
- persona judge: `>=90%` before M4 exit or first critic wiring;
- vector benchmark: `>=200k` sqlite-vec vs LanceDB before any vector implementation brief;
- memory canary/contradiction: model-backed consolidation task;
- friction thresholds: M5 soak;
- real vision/OCR: future perception-provider task;
- learned skills: pending-only until a separately gated activation task.

### 6.4 Mandatory R0.9-01 carry-ins

Include:

```text
R09-01-ARTIFACT-IDENTITY
R09-01-PROVIDER-OBSERVABILITY
R09-01-SHUTDOWN-CANCEL-CLASSIFICATION
```

Record artifact kind/label strengthening and same-byte purposes; provider attempt vs connection vs request counts; cancellation vs provider-failure classification during shutdown. Do not imply accepted paths are currently unsafe.

**(gate addition)** Also include, as its own active row (not embedded in the R0.9-03+04 entry — the ruling is general, not workflow-specific):

```text
R09-02-SERVER-TS-EXTRACTION-FIRST
```

Binding condition: `apps/gateway/src/server.ts` sits at 1,628 non-empty lines against the double-pinned `< 1,629` ceiling (headroom spent; compression exhausted). Landing gate: **the brief gate of ANY future slice that touches `server.ts`** must carry an extraction-before-addition clause; the ceiling value is never self-raised. Current evidence: the two pinning assertions in `voice.mimo-asr-provider.test.ts` and `voice.web-ptt.test.ts`.

### 6.5 Mandatory M3 deferrals

Include at least:

```text
M3-06-LOCAL-ASR
M3-STREAMING-FRAMING
M3-VAD-ENDPOINTING
M3-STREAMING-ASR-TTS
M3-LANE-A-B
M3-ACK-BANK
M3-BARGE-IN
M3-DESKTOP-TRAY
M3-LATENCY-WER-CER
```

For M3-06 preserve:

```text
status: GATED BUT DEFERRED TO FULL M3 / v1.0
model: Systran/faster-whisper-small
revision: 536b0662742c02347bc0e980a01041f333bce120
brief and gate record remain valid
```

Do not mark M3 complete.

### 6.6 Mandatory M4 deferrals

Include at least:

```text
R09-03+04-MORNING-BRIEFING
M4-SUBAGENTS
M4-BOUNDED-FANOUT
M4-GENERIC-WORKFLOW
M4-TELEGRAM
M4-UNATTENDED-7D
```

R0.9-03+04 must link its full deferral record and repeat:

```text
DEFERRED / NOT DISPATCHABLE
fresh brief required
fresh full-depth gate required
S4 >=20 required unless explicitly waived
server.ts extraction-first if any touch is necessary
```

### 6.7 Mandatory M5 deferrals

Include at least:

```text
M5-INSTALLER
M5-MCP
M5-SKILLS-HOOKS
M5-DASHBOARD
M5-COMPLETE-HARDENING
M5-EXTENSION-DOCS
M5-SOAK-2W
M5-V1
```

Distinguish developer preview, future productization, and v1.0 exit.

### 6.8 Accepted deviations and notes

Use a separate section, not the active-deferral table.

#### `DEV-R09-05-SECOND-VOICE-ATTEMPT`

Record two bounded complete attempts, one intended accepted session, one accidental session caused by incorrect microphone state, only one S4 row, Fable acceptance, no rerun, and no future landing task.

#### `DEV-R09-05-SCREENSHOT-PATH`

Record screenshot 2 clean; screenshots 1 and 3 contain a repository absolute-path prompt line; no username/PII; benign acceptance; canonical evidence preserved; public derivative should crop; no replacement here.

#### `NOTE-R09-02-ASR-QUALITY`

Record non-empty but imperfect bilingual transcripts, no WER/CER claim, and future M3 benchmark ownership.

### 6.9 Settled release-gate decisions

Record that R0.9-06′ settled:

```text
R0.9-02 D1 plan authority
external/internal naming
screenshot evidence handling
Tier-2 non-activation
```

These are not active deferrals after implementation.

---

## 7. Deliverable C — Top-level release overlays

Modify:

```text
docs/ROADMAP.md
docs/PRD.md
docs/ARCHITECTURE.md
```

Use bounded overlay sections; do not rewrite the full documents.

### 7.1 ROADMAP overlay

Add near the top:

```text
## Current release overlay — OpenFairy v0.9 Developer Preview
```

State:

- R0.9 is a release overlay, not milestone renumbering;
- R0.9-01/02/05′ are closed;
- R0.9-06′ is the release gate until countersigned;
- current S4 is `4/20`;
- full M3/M4/M5 exits remain open;
- the deferral ledger is binding;
- shipped voice is non-streaming artifact-backed cloud ASR/TTS through authenticated localhost Web;
- future milestone sections remain target state.

Do not alter M3/M4/M5 exit criteria to make v0.9 pass them.

### 7.2 PRD overlay

Add near the top:

```text
## Current implementation overlay — v0.9 Developer Preview
```

Distinguish:

**Implemented now**

- text/session/replay spine;
- governance and labels;
- memory/research primitives;
- sandbox/tool path;
- non-streaming MiMo file ASR;
- MiniMax MP3 TTS;
- authenticated localhost Web PTT;
- doctor/source-first dev launcher;
- three demo scenarios.

**Still target-state**

- streaming voice;
- VAD/endpointing;
- Lane A/B;
- barge-in;
- local ASR;
- tray/IM;
- subagents;
- workflows/scheduler;
- installer;
- production hardening;
- v1.0 metrics.

Do not change underlying PRD requirements into complete claims.

### 7.3 ARCHITECTURE overlay

Add near the top:

```text
## Current v0.9 topology overlay
```

State that existing architecture diagrams and §5.2 describe full target architecture unless marked current.

Implemented v0.9 path:

```text
authenticated localhost browser
→ AudioContext PCM capture
→ complete 16 kHz mono PCM16 WAV
→ gateway-owned input artifact
→ supervised MiMo ASR worker
→ speech.asr.final
→ existing voice-to-turn path
→ one TurnRunner
→ visible final
→ supervised MiniMax TTS worker
→ MP3 speech artifact
→ authenticated browser playback
→ canonical JSONL replay
```

Current clients:

```text
CLI
authenticated localhost Web
```

Not current:

```text
desktop tray
IM channels
remote/LAN clients
Opus streaming
partial ASR
VAD/endpointing
Lane A/B
ack bank
barge-in
```

Also record source-first TypeScript before M5; gateway ownership; browser projection discipline; provider dialect containment; and no raw/base64 audio in JSONL.

---

## 8. Deliverable D — README and demo reconciliation

Modify:

```text
README.md
docs/demo/v0.9-demo-script.md
docs/demo/assets/README.md
```

### 8.1 README

Update current status from `3/20` to `4/20` and explicitly say S4 is not complete.

Preserve:

- `OpenFairy v0.9 Developer Preview`;
- not production-ready;
- no complete M3/M4/M5 or v1.0 claim;
- Stop is local playback only;
- no ASR benchmark claim;
- R0.9-03+04 deferred;
- M3-06 deferred.

Add links to:

```text
docs/v0.9-deferrals.md
docs/v0.9/OpenFairy-v0.9-final-tiered-plan.md
```

Explain that the deferral ledger contains binding landing gates. Do not alter runtime commands/owner-live consent rules unless correcting a verified error.

### 8.2 Demo script

Reconcile the ledger statement:

```text
R0.9-02 closed at 3/20.
R0.9-05′ advanced the accepted ledger to 4/20.
S4 remains incomplete.
```

Preserve provider-consent/cleanup instructions. Do not rerun or redesign scenarios.

### 8.3 Screenshot record

Update assets README from pending capture to completed capture:

```text
01-doctor-pass.png          present
02-web-voice-roundtrip.png present
03-replay-governance.png   present
```

Add D2 deviation note.

Do not modify the PNGs or `tasks/owner-checks/R0.9-05/screenshots.json`; existing hashes must remain valid.

---

## 9. Deliverable E — Strengthen existing release-policy regression

Modify only the repository/demo-policy section of:

```text
apps/cli/test/developer-preview.test.ts
```

The suite remains `developer-preview.launch-v0` with exactly Cases A–L. Do not add Case M, rename/remove/skip/weaken Cases, or increase timeouts.

### 9.1 Case L updates

Case L must continue existing runtime/example assertions and prove:

1. README current ledger is `4/20`;
2. README refuses `20/20` and S4 completion;
3. demo script distinguishes historical `3/20` from current `4/20`;
4. `docs/v0.9-deferrals.md` exists and contains all mandatory IDs;
5. ledger rows contain landing gates/no-silent-waiver law;
6. ROADMAP contains current release overlay;
7. PRD contains current implementation overlay;
8. ARCHITECTURE contains current v0.9 topology overlay;
9. canonical plan contains 60-second contract/derived constants;
10. temporary amended plan is absent after D1;
11. assets README records capture complete/path deviation;
12. all three PNG paths remain exact;
13. current-release sections do not claim production-ready, complete M3/M4/M5, S4 `20/20`, or v1.0;
14. R0.9-03+04 remains deferred/not dispatchable;
15. M3-06 remains gated-but-deferred with pinned revision;
16. `server.ts` hash/line pin remain unchanged;
17. no runtime source gained workflow/subagent/release-only behavior.

Use deterministic filesystem assertions. Do not depend on Git history beyond the checkout.

---

## 10. Deliverable F — Work report

Create:

```text
tasks/R0.9-06-work.md
```

Required sections:

1. baseline commit;
2. gated decisions D1–D4;
3. changed-file inventory;
4. release/docs policy, not runtime;
5. canonical-plan merge procedure;
6. 60-second contract proof;
7. deleted amendment rationale/provenance;
8. release naming;
9. current ledger derivation from four rows;
10. historical `3/20` vs current `4/20`;
11. complete deferral matrix by stable ID;
12. accepted deviations register;
13. ROADMAP overlay;
14. PRD overlay;
15. ARCHITECTURE overlay;
16. README/demo reconciliation;
17. screenshot/hash preservation;
18. Case L before/after responsibility;
19. exact local validations;
20. four-job CI status;
21. no-provider proof;
22. no-runtime-diff proof;
23. `server.ts` byte identity;
24. `docs-zh/` untouched;
25. owner-check status;
26. known limitations;
27. carry-ins;
28. proposed countersign/docs actions.

Do not claim R0.9-06′ or Tier 1 closed before implementation commit, four green jobs, primary review, bounded owner check, evidence CI, final reconciliation if required, and Fable countersign.

---

## 11. Expected changed-file scope

Expected files:

```text
README.md

docs/ROADMAP.md
docs/PRD.md
docs/ARCHITECTURE.md
docs/v0.9-deferrals.md
docs/v0.9/OpenFairy-v0.9-final-tiered-plan.md
docs/v0.9/OpenFairy-v0.9-final-tiered-plan-60s-amended.md  [delete under accepted D1]
docs/demo/v0.9-demo-script.md
docs/demo/assets/README.md

apps/cli/test/developer-preview.test.ts

tasks/R0.9-06-work.md
```

The final list may be smaller. Any additional file requires explicit work-report justification.

---

## 12. Boundaries — do not

Do not modify:

```text
apps/gateway/src/server.ts
apps/gateway/src/**
apps/cli/src/**
apps/web/**
packages/**/src/**
workers/**
scripts/start-gateway.mjs
scripts/run-cli.mjs
.github/workflows/ci.yml
examples/fairy.v0.9.yaml
package.json
pnpm-lock.yaml
tasks/owner-checks/R0.9-02/voice-session-ledger.jsonl
tasks/owner-checks/R0.9-05/**
docs/specs/**
docs-zh/**
```

Exception: `apps/cli/test/developer-preview.test.ts` may change only as specified.

Do not:

- add dependencies;
- change package versions;
- create a tag/GitHub Release;
- change PNGs;
- alter closed owner evidence;
- append a voice ledger row;
- call MiMo/MiniMax/real model/research providers;
- set owner-live ASR/TTS flags;
- create a new session/audio artifact;
- run live browser voice;
- implement R0.9-03+04/M3-06/subagents/workflows/installer/tray;
- touch subsystem specs or `docs-zh/`;
- claim complete M3/M4/M5, v1.0, or production readiness;
- commit/push unless explicitly instructed.

No provider call is authorized even for verification.

---

## 13. Required deterministic validation

Run:

```powershell
pnpm install
pnpm lint
pnpm -r typecheck
pnpm --filter @fairy/cli test:developer-preview
pnpm --filter @fairy/cli test -- --reporter=verbose
pnpm -r test
pnpm dep-check
pnpm conformance
git diff --check
```

Also verify:

```powershell
git diff --name-only <BASELINE>...HEAD
git diff -- apps/gateway/src/server.ts
git diff -- apps/gateway/src apps/cli/src apps/web packages workers
git diff -- docs-zh
```

Expected:

```text
runtime diff:                 empty
server.ts diff:               empty
docs-zh diff:                 empty
provider calls:               0
voice ledger rows appended:   0
PNG files changed:            0
owner evidence changed:       0
```

Verify current PNGs still match committed R0.9-05 hashes. Do not use OCR.

---

## 14. CI requirements

All existing jobs remain required:

```text
verify (ubuntu-latest)
verify (windows-latest)
Python 3.11 speech floor (ubuntu-latest)
Python 3.11 speech floor (windows-latest)
```

No workflow change is expected. Existing speech/provider/doctor/dev checks remain. CI is keyless; no provider call permitted.

---

## 15. Owner release check

Owner checking begins only after implementation commit, four green CI jobs, primary review, and an approved bounded procedure.

No real provider credential or call is allowed.

### 15.1 Required observations

Confirm:

1. README naming is exact;
2. README current state is `4/20`, S4 incomplete;
3. canonical plan/deferral ledger links work;
4. ROADMAP/PRD/ARCHITECTURE separate current v0.9 from target v1;
5. every active deferral has a landing gate;
6. R0.9-03+04 is not dispatchable;
7. M3-06 remains deferred with pinned revision;
8. screenshot deviation is recorded;
9. all three screenshots render at exact paths;
10. no image/evidence/audio/config/session was regenerated;
11. no current-release link is broken.

### 15.2 Bounded owner evidence

Create:

```text
tasks/owner-checks/R0.9-06/release-owner-summary.json
```

Conceptual shape:

```json
{
  "checked_at_utc": "<UTC>",
  "commit": "<implementation commit>",
  "status": "pass",
  "external_name": "OpenFairy v0.9 Developer Preview",
  "internal_name": "Portfolio Release Track",
  "single_authoritative_plan": true,
  "amendment_file_removed": true,
  "voice_session_ledger": "4/20",
  "s4_complete": false,
  "deferral_rows_have_landing_gates": true,
  "r09_03_04_dispatchable": false,
  "m3_06_status": "gated_but_deferred",
  "screenshots_preserved": 3,
  "screenshot_path_deviation_recorded": true,
  "runtime_files_changed": 0,
  "owner_evidence_files_changed": 0,
  "provider_request_count": 0,
  "docs_zh_files_changed": 0
}
```

Do not include paths, env dumps, credentials, tokens, live provider IDs, session logs, base64, or account details. No new screenshot is required.

---

## 16. Acceptance matrix

R0.9-06′ is ready for primary review only when:

| Area | Acceptance |
|---|---|
| Scope | release/docs/test policy only |
| Baseline | exact `e39a4d6...` or approved successor |
| Naming | exact external/internal names |
| Plan authority | one canonical plan |
| 60-second contract | consolidated |
| Amendment history | provenance preserved |
| Deferral ledger | stable IDs + landing gates |
| Waiver law | explicit decision required |
| S4 | current `4/20`, incomplete |
| R0.9-03+04 | deferred/not dispatchable |
| M3-06 | gated but deferred, revision preserved |
| M2 landing gates | preserved |
| R0.9-01 carry-ins | preserved |
| M3/M4/M5 deferrals | explicit |
| ROADMAP | current-release overlay |
| PRD | current implementation overlay |
| ARCHITECTURE | current topology overlay |
| README | current claims/links |
| Demo script | historical/current count reconciled |
| Screenshots | unchanged, deviation recorded |
| Existing suite | Cases A–L count/name unchanged |
| Case L | strengthened, not weakened |
| Runtime | no production source change |
| `server.ts` | byte-identical |
| Provider traffic | zero |
| Owner evidence | unchanged |
| Voice ledger | no new row |
| Audio/live data | none added |
| Tests | required commands green |
| CI | four required jobs green |
| `docs-zh/` | untouched |
| Claims | no production/v1/full-milestone overclaim |
| Owner release check | bounded PASS |
| Final workflow | primary review + Fable countersign |

---

## 17. Review workflow

```text
1. Fable gates this brief and records D1–D4.
2. Owner commits gated brief + gate record.
3. Codex implements from exact committed state.
4. Owner commits/pushes implementation.
5. Four-job CI passes.
6. Primary review checks Git truth.
7. Owner performs bounded release check.
8. Owner commits/pushes bounded evidence.
9. Evidence CI passes.
10. Final primary reconciliation if required.
11. Fable performs lightweight countersign/docs pass.
12. Only then may R0.9-06′ and Tier 1 be CLOSED.
```

No provider rerun occurs anywhere.

---

## 18. Report back

Codex must report:

- base/current commit;
- exact changed files;
- D1–D4 implementation;
- plan consolidation/deletion proof;
- 60-second contract proof;
- release naming;
- ledger derivation;
- deferral IDs/landing-gate matrix;
- deviation record;
- ROADMAP/PRD/ARCHITECTURE overlays;
- README/demo updates;
- screenshot/hash preservation;
- Case L changes and test count;
- validation and CI;
- `server.ts` blob/SHA/bytes/line count;
- runtime/owner-evidence/PNG/ledger/docs-zh zero diffs;
- provider request count `0`;
- owner-check status;
- limitations/carry-ins;
- confirmation R0.9-06′ and Tier 1 are not self-closed.

Do not commit or push.

---

## 19. Dispatch condition

This draft is not dispatchable.

Dispatch requires a Fable gate record that:

- accepts/edits D1–D4;
- confirms allowed plan-file deletion;
- confirms screenshot handling;
- confirms no runtime/provider work;
- confirms expected changed-file scope;
- confirms owner release-check floor.

After the gated brief and gate record are committed, Codex may be dispatched from that exact committed state.
