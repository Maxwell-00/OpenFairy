# R0.9-06′ Brief Gate — v0.9 Developer Preview Release Gate + Deferral Ledger

Gate date: 2026-07-16
Reviewer role: task-brief gate (light tier — docs/release policy, no runtime scope).
Brief under review: `tasks/R0.9-06-prime-release-gate-deferral-ledger.md` (committed; integrity-checked — 1038 lines, no NUL)
Repo state: R0.9-05′ closed at `e39a4d6`. Tier 1 is complete pending this release gate.

---

## Verdict

**ACCEPTED — two patches applied (status line + one ledger-ID addition); all four D-decisions accepted as the drafter recommended; one scoped ownership exception recorded. Dispatch-ready.**

The brief correctly assembles every item this seat has routed to the release gate across eleven closes: the R0.9-02 gate-D1 plan consolidation, the full deferral ledger with the M2 register's seven landing gates restated verbatim (S4 4/20 honest), R0.9-01's three carry-ins, the M3/M4/M5 deferral matrices with M3-06's pinned revision preserved, both R0.9-05′ accepted deviations plus the ASR-quality note, the naming ruling, the ROADMAP/PRD/ARCHITECTURE overlays deferred since M3-05, and a Case-L regression that turns the whole release policy into standing assertions (README can never claim 20/20; the amended plan file must be absent; `server.ts` pins unchanged; no production/v1.0 claims). "Silence is never a waiver" is now ledger law — the M2 exit review's discipline, generalized.

## Gate decisions

### D1 — Plan consolidation with deletion: **ACCEPTED as recommended**

Merge the 60-second contract into the canonical plan → verify content parity → delete the temporary amendment file, with a mandatory provenance note (90s → R0.9-02 gate D1 → consolidated here) and closed reviews untouched. This completes the R0.9-02 gate's own D1, which explicitly scheduled consolidation for this release gate. The deletion is authorized: git history preserves the file at every commit that referenced it; the owner executes the deletion via their own commit. Codex must show the parity-comparison method in the work report.

### D2 — Screenshots: **ACCEPTED as recommended — supersedes this seat's recrop suggestion**

Canonical owner-evidence PNGs stay byte-identical to what was countersigned; the accepted deviation is recorded in `assets/README.md` with crop-guidance for public derivatives. Better archival posture than editing evidence after countersign — the R0.9-05′ note offered either path; this one is right.

### D3 — Version identity: **ACCEPTED** (documentation only; external "OpenFairy v0.9 Developer Preview", internal "Portfolio Release Track"; no package versions, no publishing, no tags in-task — an annotated tag afterward is the owner's separate act).

### D4 — Tier-2 non-activation: **ACCEPTED** (R0.9-03+04 stays DEFERRED / NOT DISPATCHABLE; M3-06 stays gated-but-deferred).

### Scoped ownership exception — Deliverable C

ROADMAP/PRD/ARCHITECTURE are reviewer-owned normative docs, but Case L's design requires the overlays to exist at implementation time (the test asserts their presence in Codex's own CI). **Exception granted, narrowly**: Codex may add ONLY the three exactly-titled, clearly-delimited overlay sections ("Current release overlay…", "Current implementation overlay…", "Current v0.9 topology overlay") and may not touch anything outside them — the brief's own "do not rewrite the full documents" and "do not alter M3/M4/M5 exit criteria" clauses bound it, and the countersign will diff-check that every changed line in those three files sits inside the delimited sections. This is a recorded exception for delimited status overlays, not a transfer of docs ownership.

### Gate addition — `R09-02-SERVER-TS-EXTRACTION-FIRST` as its own ledger row

The draft embedded the server.ts extraction-first ruling inside the R0.9-03+04 entry; the ruling is general (any slice touching the file), so it now has its own active-deferral ID with the ceiling facts and the brief-gate landing clause. Patched into §6.4.

## NIT

- Case L's seventeen assertions make this the first slice whose deliverable is largely *enforced honesty* — the right shape for a release gate.
- The work report's 28 sections are heavyweight for a docs slice but every one maps to an acceptance row; fine.
- Expected changed-file scope excludes all runtime source except the single Case-L test section; the countersign treats any runtime diff outside that as an automatic stop.

## Reviewer-gate clauses for the delivery countersign (light tier)

1. Runtime untouched: zero diffs outside docs/tasks/README/demo + the Case-L section of `developer-preview.test.ts`; `server.ts` blob identical; all suites green.
2. Ledger complete: every mandatory ID present (incl. the gate-added `R09-02-SERVER-TS-EXTRACTION-FIRST`), every row carrying a named landing gate + expiry + waiver rule; no `TBD`/`later` without a gate.
3. D1 executed with parity proof; amendment file absent; provenance note present; no closed-review edits.
4. Overlay exception held: every changed line in ROADMAP/PRD/ARCHITECTURE inside the three delimited sections; no exit-criteria alterations.
5. Naming/claims: no production-ready/complete-M3/M4/M5/20-20/v1.0 claim anywhere in current-release sections (Case L proves it; countersign spot-reads).
6. D2 note present in assets/README; PNG hashes unchanged.

## Dispatch instruction

Owner: commit this gate record, dispatch to Codex. When this slice closes, Tier 1 is complete and v0.9 Developer Preview is real — with a ledger that guarantees the sprint's shortcuts all have names, owners, and expiry dates.
