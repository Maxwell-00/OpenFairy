# M2-07 Owner Checks

Save owner-run evidence here:

- `testing-compaction.txt`
- `compaction-replay.jsonl`
- `compaction-manifests.txt`
- `compaction-governance-replay.jsonl`

The deterministic CI suite uses mock providers only; no real provider keys are required.
