
---

## Addendum — final tiered plan reviewed, 2026-07-13

`docs/v0.9/OpenFairy-v0.9-final-tiered-plan.md` reviewed against every decision in this gate: tiering, invariants, the three MiMo clauses (base64 boundary / chat-dialect exception with a protocol.md note assigned to the countersign docs pass / api-key hygiene), coordinator carry-in with `server.ts` net-decrease, S4 session counting at R0.9-02 exit, Tier 2 restricted to M0-registered `plan.*`/`loop.*`/`workflow.*` types with additive payload fields only, subagent-ready seam, review-depth table, Developer Preview naming, deferral ledger structure, and the Codex do-not-implement-from-plan guard. **Faithful on all counts — APPROVED with one material fix applied in place:**

**WebM/WAV decision pulled forward (plan §5 artifact scope).** The draft deferred "WebM support?" to R0.9-02 — but Chrome/Edge `MediaRecorder` produces webm/opus by default and MiMo accepts only wav/mp3, so the deferred question was actually a mid-sprint trap (ffmpeg transcoder — forbidden — or provider change). Decided now: **the browser records/encodes WAV client-side** (PCM via `AudioContext`, ~50 dependency-free lines; implementation shape belongs to the R0.9-02 brief); WebM never enters the artifact store; no transcoder ever; UI recording cap 90 s (MiMo's 10 MB base64 ≈ 3.9 min of 16 kHz mono WAV — ample). The artifact MIME set closes at WAV + MP3 with no R0.9-02 dependency.

Owner action items stand as written (MiMo key obtained + verified against `mimo-v2.5-asr` before dispatch). Next: ChatGPT drafts the R0.9-01 brief; this seat gates it.
