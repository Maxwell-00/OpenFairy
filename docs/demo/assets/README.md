# R0.9-05′ screenshot owner gate

Codex does not fabricate product screenshots. The owner must capture and commit exactly:

```text
docs/demo/assets/01-doctor-pass.png
docs/demo/assets/02-web-voice-roundtrip.png
docs/demo/assets/03-replay-governance.png
```

Capture at normal GitHub README readability with a non-sensitive synthetic phrase. Cropping browser chrome is allowed; editing product state is not. Before committing, inspect visible pixels and metadata for zero gateway token, provider credential, secret value/reference beyond approved names, Authorization/api-key value, WebSocket query token, provider request ID/body, absolute local path, microphone device label, personal account data, sensitive transcript, DevTools network panel, audio/base64/data URL, or live-data filename.

Suggested frames:

1. doctor text report with required PASS checks and honest optional warnings;
2. transcript + visible final + MP3 control after the normal synthetic bilingual turn;
3. canonical replay/governance evidence without hidden reasoning or private content.

Do not commit audio, live config, session JSONL, artifact registry, or a GIF containing any forbidden value. Screenshot status remains **PENDING OWNER CAPTURE** until all three real PNGs pass review.
