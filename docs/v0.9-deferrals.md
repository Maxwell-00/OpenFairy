# OpenFairy v0.9 Developer Preview — deferral and accepted-deviation ledger

**External name:** OpenFairy v0.9 Developer Preview  
**Internal name:** Portfolio Release Track  
**Historical release baseline:** `e39a4d672b29bec55d01b527abc59dca9b062363`  
**Implementation baseline:** `9b2968f0ea1e11cd1868c6bf28e04c6e711c775a`  
**Last-updated implementation commit:** `06485c5507603226f94c5bdcb38712eeacedadcc`
**Scope:** Tier-1 release governance; this ledger does not authorize implementation.

Every active deferral below has an owner, named landing gate, expiry condition, and waiver rule. Carrying debt beyond its landing gate requires a new explicit decision recorded at that gate. Silence is never a waiver. Closed reviews remain the historical source of truth for accepted behavior and evidence.

## Active deferrals

| ID | Source | Status | Reason | User impact | Security / trust impact | Current evidence | Owner | Landing gate | Expiry condition | Waiver rule |
|---|---|---|---|---|---|---|---|---|---|---|
| `M2-S4-20` | `tasks/M2/M2-exit-review.md` | blocked-by-landing-gate | Real usage cannot be replaced by fixture theater. | Proactive/workflow entry remains unavailable; current ledger is 4/20. | Preserves evidence honesty; fixture sessions do not count. | Four accepted rows in `tasks/owner-checks/R0.9-02/voice-session-ledger.jsonl`; R0.9-02 historical close was 3/20. | Owner | **M4 entry gate** | Re-examine when the ledger reaches at least 20 owner real sessions or before any M4 slice is dispatched. | Only an explicit M4 entry-gate decision may alter the threshold or counting law; silence is not a waiver. |
| `M2-PERSONA-JUDGE-90` | `tasks/M2/M2-exit-review.md` | blocked-by-landing-gate | The frozen model-backed style judge and corpus do not exist yet. | Persona has deterministic consistency but no ≥90% frozen-judge claim. | Avoids manufactured model-evaluation evidence. | `persona.consistency` and `substance.invariance` remain mandatory-green. | Persona/critic task owner | **M4 exit gate, or the first critic-role wiring task, whichever comes first** | Re-examine before first critic wiring and no later than M4 exit. | Only that gate's recorded review may replace the ≥90% criterion; silence is not a waiver. |
| `M2-VECTOR-200K` | `tasks/M2/M2-exit-review.md` | blocked-by-landing-gate | No vector path exists to benchmark honestly. | Retrieval remains FTS5; no vector backend is selected. | Avoids selecting storage from synthetic claims. | `tasks/owner-checks/M2-09/vector-backend-decision.md` defines the benchmark shape. | First vector-task owner | **Brief-gate precondition of the first vector-implementation task (M3-prep at earliest, M5-hardening at latest)** | Re-examine before any vector implementation brief is dispatchable. | Only that brief gate may change the ≥200k sqlite-vec versus LanceDB comparison; silence is not a waiver. |
| `M2-MEMORY-CANARY-CONTRADICTION` | `tasks/M2/M2-exit-review.md` | gated-but-deferred | Full model-backed canary, contradiction, promotion, decay, and index maintenance exceed deterministic dream-cycle v0. | No full learned consolidation quality claim. | Keeps `memory.canary` visibly skipped instead of fake-passed. | Deterministic MemoryGate, Chronicle, and manual consolidation suites are green. | Model-backed consolidation owner | **Model-backed consolidation task (dream-cycle as a real workflow, M4)** | Re-examine before a model-backed consolidation workflow is dispatched. | Only its full gate may redefine the benchmark or carry it onward; silence is not a waiver. |
| `M2-GOVERNANCE-FRICTION-SOAK` | `tasks/M2/M2-exit-review.md` | blocked-by-landing-gate | Nightly/soak thresholds require real workload duration. | No long-duration friction-rate claim. | PR-tier zero-tolerance checks remain active while long-run trust evidence is absent. | Governance friction canary PR-tier v0 is mandatory-green. | M5 soak owner | **M5 soak** | Re-examine when M5 soak starts and before M5 exit. | Only the M5 soak gate may set or waive thresholds in a recorded decision; silence is not a waiver. |
| `M2-REAL-VISION-OCR` | `tasks/M2/M2-exit-review.md` | gated-but-deferred | M2 shipped deterministic perception tools, not a real vision provider route. | Real image description/OCR provider use is unavailable. | Prevents unreviewed image egress and provider wiring. | Artifact-backed mock `vision.describe` and `vision.ocr` coverage is green. | Perception-provider task owner | **Separately gated real perception-provider task** | Re-examine before the first real vision/OCR provider route is dispatched. | Only that full provider gate may authorize the route; silence is not a waiver. |
| `M2-LEARNED-SKILL-ACTIVATION` | `tasks/M2/M2-exit-review.md` | gated-but-deferred | Learned-skill output is intentionally pending-only. | Generated skill drafts cannot execute. | Prevents unreviewed generated behavior from becoming active. | Drafts land only under `extensions/skills/learned/pending/`. | Learned-skill task owner | **Separately gated learned-skill activation task** | Re-examine before any pending learned skill can be enabled or loaded. | Only that activation gate may authorize execution; silence is not a waiver. |
| `R09-01-ARTIFACT-IDENTITY` | `tasks/R0.9-01-review.md` carry-in C1 | deferred | Content-addressed IDs are byte-derived; kind, stronger labels, region metadata, and same-byte purposes need neutral semantics. | Same bytes cannot silently gain a differently governed purpose; weaker collisions fail closed. | Current fail-closed checks preserve label and kind safety. | R0.9-01 rejects weaker labels, wrong kind, and missing/wrong region metadata. | Neutral artifact-store owner | **First neutral artifact-store task that changes identity or registry semantics** | Re-examine before identity, label merge, registry update, or same-byte purpose behavior changes. | Only that task's gate may change fail-closed semantics; silence is not a waiver. |
| `R09-01-PROVIDER-OBSERVABILITY` | `tasks/R0.9-01-review.md` carry-in C2 | deferred | Attempt, connection, and request telemetry are not fully separated on every worker-local failure. | Diagnostics may be less granular outside accepted success/denial paths. | Does not weaken pre-I/O denial or leak provider data. | Accepted paths expose bounded connection/request counts; zero-denial is transport-observed. | Provider telemetry owner | **First provider-observability task changing attempt/connection/request counters** | Re-examine before new provider-count telemetry is canonicalized or used for release evidence. | Only that task's gate may redefine count semantics; silence is not a waiver. |
| `R09-01-SHUTDOWN-CANCEL-CLASSIFICATION` | `tasks/R0.9-01-review.md` carry-in C3 | deferred | Some shutdown timing paths may classify termination as provider failure rather than cancellation. | Shutdown diagnostics can be less precise while cleanup remains bounded. | Worker/staging cleanup and cancellation safety remain intact. | R0.9-01 shutdown is bounded with zero worker and staging residue. | Speech lifecycle owner | **First speech-lifecycle task modifying shutdown or cancellation classification** | Re-examine before speech shutdown/cancel evidence categories change. | Only that task's gate may change classification; silence is not a waiver. |
| `R09-02-SERVER-TS-EXTRACTION-FIRST` | R0.9-02 close and R0.9-06′ gate addition | blocked-by-landing-gate | `server.ts` headroom is spent; compression is exhausted. | New server responsibilities cannot be added in place. | Extraction-first preserves reviewability and the single-gateway boundary. | `server.ts` is 1,628 non-empty lines against `<1,629`; both MiMo ASR and Web PTT suites pin it. | Owner of any touching slice | **Brief gate of ANY future slice that touches `apps/gateway/src/server.ts`** | Re-examine on every proposed `server.ts` touch. | That gate must require extraction before addition; the ceiling is never self-raised and silence is not a waiver. |
| `M3-06-LOCAL-ASR` | `tasks/M3/M3-06-faster-whisper-local-asr.md` and brief review | gated-but-deferred | Local ASR is outside Tier 1 and needs its existing dependency/model supply-chain gate. | Offline local transcription is unavailable. | Deferral avoids downloading or running an ungated model. | Valid gated brief; model `Systran/faster-whisper-small`, immutable revision `536b0662742c02347bc0e980a01041f333bce120`. | M3-06 owner | **Full M3 / v1.0 M3-06 dispatch gate** | Re-examine only from a fresh authorized dispatch against the gated brief and current Git state. | Only a new recorded dispatch/gate decision may activate M3-06; silence is not a waiver. |
| `M3-STREAMING-FRAMING` | `docs/ROADMAP.md` M3 and v0.9 plan | gated-but-deferred | Tier 1 deliberately uses complete-file audio. | No compact/streaming audio transport. | Avoids an unaudited second framing protocol. | Current authenticated Web uploads one complete WAV artifact. | M3 protocol owner | **M3 streaming-framing protocol gate** | Re-examine before any streaming audio frame crosses a client/worker boundary. | Only that protocol gate may authorize framing; silence is not a waiver. |
| `M3-VAD-ENDPOINTING` | `docs/ROADMAP.md` M3 and v0.9 plan | gated-but-deferred | Complete-file PTT has an explicit user stop and fixed hard limit. | No automatic voice activity detection or endpointing. | Avoids silent capture-state and timing behavior. | 60-second sample-count contract and explicit stop/auto-stop are implemented. | M3 endpointing owner | **M3 VAD/endpointing gate after streaming framing is settled** | Re-examine before automatic speech start/end decisions are introduced. | Only that gate may authorize endpointing; silence is not a waiver. |
| `M3-STREAMING-ASR-TTS` | `docs/ROADMAP.md` M3 and v0.9 plan | gated-but-deferred | Current providers are bounded non-streaming adapters. | No partial transcript or incremental speech playback. | Keeps raw/base64 audio out of NDJSON/JSONL and preserves worker isolation. | MiMo complete-file ASR and MiniMax non-streaming MP3 TTS are closed. | M3 speech-provider owner | **M3 streaming ASR/TTS provider gate** | Re-examine after framing and before any streaming provider I/O is implemented. | Only that full provider gate may authorize streaming; silence is not a waiver. |
| `M3-LANE-A-B` | `docs/ROADMAP.md` M3 and v0.9 plan | gated-but-deferred | Tier 1 preserves one TurnRunner and one voice-to-turn path. | No fast conversational lane beside the agentic lane. | Avoids concurrent state and cancellation ambiguity. | One canonical ASR final feeds one existing TurnRunner path. | M3 voice-integration owner | **M3 Lane A/B turn-integration gate** | Re-examine before a second response lane or concurrent voice turn is introduced. | Only that gate may authorize lane semantics; silence is not a waiver. |
| `M3-ACK-BANK` | `docs/ROADMAP.md` M3 and v0.9 plan | gated-but-deferred | Acknowledgement clips depend on settled two-lane behavior. | No acknowledgement bank during longer work. | Avoids prerecorded response selection outside canonical turn law. | Current visible final alone derives TTS input. | M3 voice UX owner | **M3 acknowledgement-bank gate after Lane A/B** | Re-examine before any acknowledgement clip is selected or played. | Only that gate may authorize an acknowledgement bank; silence is not a waiver. |
| `M3-BARGE-IN` | `docs/ROADMAP.md` M3 and v0.9 plan | gated-but-deferred | Browser Stop is local playback control only. | User cannot interrupt the active model/TTS path by speaking. | Avoids false cancellation guarantees. | Docs and tests state Stop is not barge-in. | M3 interruption owner | **M3 barge-in cascade gate** | Re-examine before voice input cancels playback, TTS, TurnRunner, or tools. | Only that gate may authorize interruption semantics; silence is not a waiver. |
| `M3-DESKTOP-TRAY` | `docs/ROADMAP.md` M3 and v0.9 plan | gated-but-deferred | Tier 1 clients are CLI and localhost Web. | No resident desktop tray client. | Avoids remote bind, installer, and OS lifecycle surface. | Source-first CLI/dev and authenticated localhost Web are closed. | M3 client owner | **M3 desktop-client gate** | Re-examine before a tray process, global hotkey, or desktop audio client is added. | Only that client gate may authorize it; silence is not a waiver. |
| `M3-LATENCY-WER-CER` | `docs/ROADMAP.md` M3 and `docs/specs/evals.md` | blocked-by-landing-gate | Tier 1 owner checks are functional smoke evidence, not performance/accuracy benchmarks. | No p50/p95, WER, or CER release claim. | Prevents imperfect transcripts from being misrepresented as measured quality. | Non-empty bilingual transcript evidence exists; no WER/CER claim. | M3 benchmark owner | **M3 exit benchmark gate** | Re-examine when representative corpora and latency harnesses exist, before M3 exit. | Only the M3 exit gate may set or waive benchmark thresholds; silence is not a waiver. |
| `R09-03+04-MORNING-BRIEFING` | [full deferral record](../tasks/R0.9-03+04-DEFERRED-bounded-morning-briefing.md) | blocked-by-landing-gate | Tier 2 was not activated and workflow authority was not granted. | No automated Morning Briefing. | Prevents unattended provider/tool execution without approval and budget law. | **DEFERRED / NOT DISPATCHABLE**; full deferral record remains authoritative. | Owner plus full-depth gate reviewer | **Fresh R0.9-03+04 brief and fresh full-depth gate after S4 ≥20 unless explicitly waived** | Re-examine only after Tier-1 close, S4 gate satisfaction/explicit decision, current-state inspection, and owner dispatch. | Fresh gate must explicitly record any S4 waiver and require extraction-first for any `server.ts` touch; silence is not a waiver. |
| `M4-SUBAGENTS` | v0.9 plan SA-01 and `docs/ROADMAP.md` M4 | gated-but-deferred | Tier 1 has one TurnRunner and no subagent execution contract. | No delegated agent execution. | Avoids unbounded authority and hidden concurrent work. | No production subagent runtime exists. | Subagent-contract owner | **M4/SA-01 subagent execution-contract and registry gate** | Re-examine before any child-agent runtime can execute. | Only that full gate may authorize subagents; silence is not a waiver. |
| `M4-BOUNDED-FANOUT` | v0.9 plan SA-03 and `docs/ROADMAP.md` M4 | gated-but-deferred | Fan-out requires a settled subagent contract and explicit concurrency budgets. | No parallel delegated execution. | Avoids unbounded cost, data egress, and cancellation ambiguity. | No fan-out runtime exists. | Fan-out task owner | **M4/SA-03 bounded-fan-out gate after SA-01/SA-02** | Re-examine before concurrency greater than one is enabled. | Only that gate may set concurrency/budget law; silence is not a waiver. |
| `M4-GENERIC-WORKFLOW` | `docs/ROADMAP.md` M4 | gated-but-deferred | Tier 1 contains no scheduler or workflow runtime. | No reusable autonomous workflow execution. | Avoids a second truth store and unattended authority. | Existing turn/tool path remains owner-initiated. | Workflow runtime owner | **M4 generic-workflow runtime gate** | Re-examine before durable workflow execution state or scheduling is added. | Only that gate may authorize workflow runtime behavior; silence is not a waiver. |
| `M4-TELEGRAM` | `docs/ROADMAP.md` M4 | gated-but-deferred | Current clients are loopback CLI/Web only. | No Telegram/IM access. | Avoids remote identity, transport, and token boundaries. | Gateway binds loopback and Web requires auth. | First IM-client owner | **M4 first-IM-client gate** | Re-examine before any non-loopback message ingress/egress. | Only that client gate may authorize the channel; silence is not a waiver. |
| `M4-UNATTENDED-7D` | `docs/ROADMAP.md` M4 | blocked-by-landing-gate | No autonomous runtime exists to soak honestly. | No seven-day unattended reliability claim. | Avoids fabricated safety/recovery evidence. | Current dev launcher has bounded owned-child cleanup. | M4 soak owner | **M4 exit seven-day unattended soak gate** | Re-examine after approved workflow runtime exists and before M4 exit. | Only the M4 exit gate may change the seven-day evidence floor; silence is not a waiver. |
| `M5-INSTALLER` | `docs/ROADMAP.md` M5 and v0.9 plan | gated-but-deferred | Developer preview remains source-first. | No installer, service, auto-update, or packaged onboarding. | Avoids privileged OS mutation and unsupported upgrade behavior. | `pnpm fairy dev` is the source-first developer path. | M5 packaging owner | **M5 installer/packaging gate** | Re-examine before producing any installable artifact or system service. | Only that gate may authorize packaging; silence is not a waiver. |
| `M5-MCP` | `docs/ROADMAP.md` M5 | gated-but-deferred | Extension protocol surface is outside Tier 1. | No MCP extension compatibility claim. | Avoids exposing unreviewed tool authority. | Existing tools use repository contracts. | M5 extension owner | **M5 MCP extension-surface gate** | Re-examine before an MCP server/client surface is shipped. | Only that gate may authorize MCP behavior; silence is not a waiver. |
| `M5-SKILLS-HOOKS` | `docs/ROADMAP.md` M5 | gated-but-deferred | Runtime skills/hooks need a separate trust model. | No active third-party skill/hook ecosystem. | Prevents unreviewed code activation. | Learned-skill drafts remain pending-only. | M5 extension owner | **M5 skills/hooks extension-surface gate** | Re-examine before runtime discovery or activation of skills/hooks. | Only that gate may authorize activation; silence is not a waiver. |
| `M5-DASHBOARD` | `docs/ROADMAP.md` M5 | gated-but-deferred | Tier 1 ships terminal/Web demo surfaces, not an operations dashboard. | No polished diagnostics UI. | Avoids accidental exposure of sensitive evidence. | CLI replay/doctor provide bounded diagnostics. | M5 diagnostics owner | **M5 dashboard/privacy gate** | Re-examine before browser-visible operational telemetry is added. | Only that gate may authorize dashboard data surfaces; silence is not a waiver. |
| `M5-COMPLETE-HARDENING` | `docs/ROADMAP.md` M5 | blocked-by-landing-gate | Cross-platform product hardening is not established by developer-preview CI. | Production support/stability is not promised. | Prevents developer evidence from becoming a production claim. | Ubuntu/Windows deterministic CI and bounded owner checks exist. | M5 hardening owner | **M5 complete-hardening gate** | Re-examine before any production-ready claim or support commitment. | Only that gate may accept hardening evidence; silence is not a waiver. |
| `M5-EXTENSION-DOCS` | `docs/ROADMAP.md` M5 | gated-but-deferred | Extension APIs are not frozen. | No complete extension-author documentation. | Avoids documenting unstable authority surfaces as safe. | Current docs cover implemented developer-preview boundaries. | M5 documentation owner | **M5 extension-API freeze and documentation gate** | Re-examine after extension contracts freeze and before M5 exit. | Only that gate may declare extension docs complete; silence is not a waiver. |
| `M5-SOAK-2W` | `docs/ROADMAP.md` M5 | blocked-by-landing-gate | Two-week product soak requires a hardened candidate. | No two-week reliability claim. | Avoids simulated operational assurance. | Deterministic suites are green; long-run product soak is absent. | M5 soak owner | **M5 exit two-week soak gate** | Re-examine when a hardened release candidate starts its soak. | Only the M5 exit gate may change the soak floor; silence is not a waiver. |
| `M5-V1` | `docs/ROADMAP.md` M5 | blocked-by-landing-gate | v0.9 Developer Preview does not satisfy full milestone exits. | No v1.0 product or production-readiness claim. | Keeps release identity and user expectations honest. | External identity is OpenFairy v0.9 Developer Preview; no tag/release is created here. | Owner and M5 release reviewer | **M5 final exit and v1.0 release gate** | Re-examine only after all M3/M4/M5 exits, hardening, docs, and soak gates are resolved. | Only the final recorded release decision may authorize v1.0; silence is not a waiver. |

### Binding status restatements

`R09-03+04-MORNING-BRIEFING` remains:

```text
DEFERRED / NOT DISPATCHABLE
fresh brief required
fresh full-depth gate required
S4 >=20 required unless explicitly waived
server.ts extraction-first if any touch is necessary
```

`M3-06-LOCAL-ASR` remains `GATED BUT DEFERRED TO FULL M3 / v1.0`, with model `Systran/faster-whisper-small` at immutable revision `536b0662742c02347bc0e980a01041f333bce120`. Its existing brief and gate record remain valid but do not authorize dispatch.

## Accepted deviations and limitation note

### `DEV-R09-05-SECOND-VOICE-ATTEMPT`

The authorized R0.9-05′ owner procedure made two bounded complete provider attempts: one intended accepted session and one accidental attempt caused by incorrect microphone state. Only one S4 row was accepted from that procedure. Fable accepted the disclosure, required no rerun, and assigned no landing task; this is an archived deviation, not an active deferral.

### `DEV-R09-05-SCREENSHOT-PATH`

Screenshot 2 is clean. Screenshots 1 and 3 contain a PowerShell prompt line with the repository absolute path, but no username, PII, credential, token, request ID, or device label. R0.9-05′ accepted the line as benign. Canonical evidence remains unchanged; a public derivative should crop that prompt/path line rather than overwrite the evidence.

### `NOTE-R09-02-ASR-QUALITY`

Owner evidence contains non-empty but imperfect bilingual transcripts. R0.9 makes no WER/CER or ASR-accuracy claim. Representative accuracy and latency measurement belongs to `M3-LATENCY-WER-CER` at the M3 benchmark gate.

## Settled R0.9-06′ gate decisions

R0.9-06′ settled the R0.9-02 D1 plan-authority debt by consolidating the binding 60-second amendment into one canonical plan; fixed the external/internal names; preserved canonical screenshot evidence while documenting public crop guidance; and kept Tier 2 inactive. These decisions are settled release policy, not active deferrals and not implementation authority.
